# VBA-WebView2 開発メモ 第5部 — K 軸 (基盤・借金返済)

全機能の土台にあたる部分と、後回しにしてきた借金を扱う軸。
サンク層 (`Wv2Thunks.bas`)、Sentinel、そして開発を支えるログ基盤がここに入る。

D 軸 (機能を前へ進める) や E 軸 (開発環境) と違い、**K 軸は「進まないための投資」**
という性格を持つ。目に見える機能は増えないが、これが無いと先の検証が成り立たない。

**現役の追記先。**

---

## K-1 段階 — デバッグログのファイル出力 (`Wv2Log`)

### 位置づけ

D-2 の実機検証で、**イミディエイトウィンドウの保持行数が足りず前半のログが流れて
確認できない**事態が起きた。ハンドルの連番から健全性は確定できたが、
★流れたことに気づけない★のが本質的な危険だった。

D 軸は 1 テストで数十往復するので今後必ず再発する。**以降のすべての検証の土台。**
たーぼーさん自身が「テキストファイルに出力する仕組みとか作った方が良くないか？」と
提案し、D-2 完了時点で「次はこれ」と合意していた宿題。

E-3 で `import.ps1 -AllowNew` が動くようになったため、`Wv2Log.bas` を
**VBE で手作りせずに投入できる**状態になってから着手した。

### 論点と決定 (9 件を一括合意)

| # | 論点 | 決定 |
|---|---|---|
| 1 | 出力先 | `%APPDATA%\Wv2Browser\logs\`。`settings.txt` と同じ根に揃える。ブックと同じ場所は **OneDrive の同期と衝突しうる**ので避けた |
| 2 | ファイルの切り方 | **起動ごとに 1 本** (`wv2_yyyymmdd_hhnnss.log`)。20 本を残して古いものは自動削除。D 軸は「1 テスト = 数十往復」なので、**テスト 1 回分が 1 ファイルに閉じている**のが検証に効く |
| 3 | ログレベル | 4 段階 (`LOG_ERROR` / `LOG_WARN` / `LOG_INFO` / `LOG_DEBUG`)。しきい値は実行時に変更可。**移行期の既定は `LOG_DEBUG`** (既存の `Debug.Print` 相当を落とさない) |
| 4 | `Debug.Print` との併記 | ★`LogPrint` が内部で `Debug.Print` も撃つ★ 呼び出し側は 1 行だけ。これで**機械的な 1 対 1 置換**で移行でき、イミディエイトの見え方も変わらない |
| 5 | 移行の範囲 | `Wv2Pane` の D 軸まわりだけ。他は次回以降 (**既存の全 `Debug.Print` を一度に書き換えない**) |
| 6 | 書き込みの方式 | **開きっぱなしで毎行書く。** `ERROR` / `WARN` は直後に強制フラッシュ。メモリに溜めて終了時に書く案は、仕様事実20 でクラッシュが現実的な領域なので却下 |
| 7 | 再入対策 | **カウンタ + 保留キュー** (設計原則78 と同じ型)。再入中の行を捨てると「流れたことに気づけない」という K-1 の目的そのものを裏切るので、積んで抜けたときに書く |
| 8 | 文字コード | ログは UTF-8 / BOM なし / CRLF。`Wv2Tests.WriteUtf8NoBom` は**再利用しない** (`Wv2Tests` は「検証専用。移植時に外せる」位置づけなので、製品コードが依存してはいけない) |
| 9 | 識別子の命名 | ★1 文字の識別子は避ける★ 仕様事実43 の的になる。`LogE` / `LogW` / `LogI` / `LogD` を採用 (`E` / `W` / `I` / `D` は却下) |

### `Wv2Log` の API

```vba
Wv2Log.LogE "..."      ' ERROR (書いた直後に強制フラッシュ)
Wv2Log.LogW "..."      ' WARN  (同上)
Wv2Log.LogI "..."      ' INFO
Wv2Log.LogD "..."      ' DEBUG

?Wv2Log.LogPath        ' 今のログファイルのフルパス
Wv2Log.LogLevel = LOG_WARN   ' しきい値
Wv2Log.LogEcho = False       ' イミディエイトへの併記を止める
Wv2Log.LogStart        ' 新しいログに切り替える (検証の仕切り直し)
Wv2Log.LogStop / LogFlush
```

出力はこうなる。

```
=== Wv2Log 開始 2026-08-20 08:36:20 ===
=== path = C:\Users\gugug\AppData\Roaming\Wv2Browser\logs\wv2_20260820_083620.log ===
=== 行頭の 6 桁は連番。欠番があればログが落ちている ===
000001 2026-08-20 08:36:20.033 [ERR ] K-1: ERROR の行
000002 2026-08-20 08:36:20.033 [WARN] K-1: WARN の行
```

### ★行に連番を振った★ (論点に無かった追加)

**K-1 の動機は「流れたことに気づけない」ことなので、気づける仕掛けを入れないと
問題を半分しか解いていない。** 行頭の 6 桁を見て欠番があれば落ちている。

検証 Sub もこれを見ており、「連番に欠番が無い」を合否条件に入れてある。

### 実装前に済ませた検査

- **識別子の衝突検査を新設** (`tools/check_idents.py`)。既存 748 種と小文字化して照合。
  `Wv2Log` で使う 50 個の識別子すべてが衝突なしであることを確認してから書き始めた。
- **UTF-8 エンコーダを Python で先に再現**し、**U+0020〜U+FFFF 全部** +
  補助面を間引いた総当たりで Python の `encode('utf-8')` と一致することを確認。
- **整形・構文検査を新設** (`tools/check_vba.py`)。定型検査 1 番の常設化。

### ★踏んだ罠: 設計原則29 (危うくサロゲート判定が死ぬところだった)★

サロゲートペアの判定に書いた `&HD800` / `&HDBFF` / `&HDC00` / `&HDFFF` は、
いずれも `&H7FFF` を超える。**`&` サフィックスが無いと負の Integer として評価され、**

```vba
If logCode >= &HD800 And logCode <= &HDBFF Then
```

**この条件が永久に成立しない。** 4 バイト文字が黙って壊れる、目視では気づけない不具合。
新設した `check_vba.py` が検出し、コードを渡す前に潰せた。

★さらに import 後に裏付けが取れた★ VBE は `&H10000&` / `&H40000&` の**冗長な `&` を
落とした**のに、`&HD800&` / `&HDC00&` は**残した**。
**VBE 自身が「危険域は 0x8000〜0xFFFF だけ」と判断している。** 検査器の閾値と一致した。

### 実機検証の結果 — 3 回かかった

★1 回目 4/13、2 回目 13/14、3 回目 14/14 で合格。
2 回とも落ちた原因は `Wv2Log` ではなく**検証 Sub の側**だった。★

#### 1 回目 (4/13) — ADODB がログを読めない

`K1ReadUtf8: 失敗 (3002) ファイルを開けませんでした。` が出て、
読み戻しに依存する 9 項目がまとめて落ちた。

**ここで 2 つの発見があった。**

1. **ログファイル自体は最初から完璧だった。** 失敗表示に埋もれていたので、
   ファイルを直接読んで確かめた。UTF-8 / BOM なし / CRLF、連番 1〜10 に欠番なし、
   **`[😀]` が無傷**、保留キューも設計どおり (`000010` の後に `000008` `000009`)。
   ★イミディエイトの `[??]` は CP932 表示の制約にすぎず、データは壊れていない★
2. **検査 (8)(9) が「空振りで合格」していた。** 読み戻せていないのに
   `InStr("", x) = 0` で「無いことの確認」だけ通っていた。
   **発火しない検査は無い検査より悪い** (設計原則95) のに、自分の検証 Sub で
   それをやっていた。

#### 2 回目 (13/14) — `Shared` にしても ADODB は開けない

`Lock Write` を `Shared` に変えたが (3) だけ落ち続けた。
そこで**共有モードの問題か ADODB 固有の問題か**を PowerShell で切り分けた。

| 書き手の共有モード | .NET ReadAllBytes | 共有を明示する読み手 | **ADODB.Stream** |
|---|---|---|---|
| `Shared` 相当 | NG | **OK** | **NG** |
| `Lock Write` 相当 | NG | **OK** | **NG** |
| 排他 | NG | NG | NG |

**`ADODB.Stream` は共有モードに関わらず、他のハンドルが開いているファイルを開けない。**
つまり (3) は ADODB では**原理的に通らない検査**で、
★私が誤った道具で誤ったことを測っていた★。`Wv2Log` 側の欠陥ではない。

同時に「**共有を明示する読み手なら開ける**」ことも分かったので、
エディタでログを開く用途には支障がない。

#### 3 回目 (14/14) — 合格

(3) を VBA 自身の `Open ... For Binary Access Read Shared` に差し替えて全項目合格。

| # | 検証 | 結果 |
|---|---|---|
| 1〜2 | `LogPath` が取れる / ファイルが実在する | OK |
| 3 | **実行中でもログを読める (共有読み取り)** | OK |
| 4 | ログを読み戻せた | OK |
| 5〜8 | 4 レベルの行がそれぞれある | OK |
| 9〜10 | しきい値で切った INFO / DEBUG が無い | OK |
| 11 | **サロゲートペアが無傷** | OK |
| 12 | 日本語が無傷 | OK |
| 13 | **再入中の 2 行が後から流れている** | OK |
| 14 | **連番に欠番が無い (10 行 / 最大 10)** | OK |

### 実装上の事後報告 (設計原則79)

1. **行頭に 6 桁の連番を振った。** 論点に無かったが、K-1 の動機に直結するため。
2. **ログファイルを `Shared` で開くようにした。** 実行中でもエディタで読めるように。
   当初は `Lock Write` にしていたが、読み手も弾かれた。
3. **`Wv2Log.Debug_SetLogDepth` を追加。** 再入ガードの検証用フック
   (`Wv2Pane.Debug_SetInCallback` と同じ型)。製品コードからは呼ばない。
4. **検証 (14) を「連番の順序」ではなく「欠番の有無」に変更。**
   再入テストは保留キューを後から流すので順序はわざと入れ替わる。
   K-1 の動機は順序ではなく欠落なので、こちらが正しい。
5. **検査ツールを 2 本、常設化した** (`check_idents.py` / `check_vba.py`)。
   設計原則95 に沿って、踏んだ制約をその場で直さず検査器にした。

### 変更ファイル

| ファイル | 内容 |
|---|---|
| `src/Wv2Log.bas` | **新規 465 行** |
| `src/Wv2Pane.cls` | `EvalSync` (9 本) と `ClearElementRegistry` (2 本) の `Debug.Print` を移行。★ロジックは 1 行も変えていない★ |
| `src/Wv2Tests.bas` | `Test_K1_Log` / `Test_K1_Help` / `K1Bool` / `K1ReadUtf8` / `K1CanReadWhileOpen` |
| `tools/check_idents.py` | 新規 |
| `tools/check_vba.py` | 新規 |

**移行したのは 11 本だけ。`src/` にはまだ 1,623 本の `Debug.Print` が残っている。**
段階的に移す。

### 確立した設計原則 (99〜100)

## ★ 設計原則 99 — 「無いことの確認」は、対象を読めたことを確かめてから行う ★

空文字に対する検索は何でも「無い」と答える。読み取りに失敗した状態で
「〜が無いこと」を確認すると、**壊れているのに合格する。**

K-1 の 1 回目で実際にこれを踏んだ。ログを読み戻せていないのに
「しきい値で切った INFO が無い」が合格していた。

対策は単純で、**読めたことを先に確かめ、読めなければ以降の照合を打ち切る。**
打ち切ったことを出力に明記すれば、合格数の水増しにもならない。

## ★ 設計原則 100 — 検証が落ちたら、まず「測り方」が対象を測れているかを疑う ★

K-1 は 2 回続けて落ちたが、**2 回とも原因は検証 Sub の側**で、
`Wv2Log` は最初から正しく動いていた。

- 1 回目: 読み取りツール (ADODB) がファイルを開けなかっただけ
- 2 回目: そのツールでは**原理的に測れない性質**を測ろうとしていた

道具の制約を対象の欠陥と取り違えると、正しいものを壊しに行ってしまう。
**落ちた項目の周辺を、対象とは別の経路で直接確かめる**のが最短の切り分けになる
(K-1 では、ログファイルを Python で直接読んで「中身は完璧」と即断できた)。

### 確立した仕様事実 (44〜46)

## ★ 仕様事実 44 — `ADODB.Stream.LoadFromFile` は他のハンドルが開いているファイルを開けない ★

★共有モードに関わらず開けない★ (実測)。書き手が `Shared` (ロックなし) で
開いていても `ファイルを開けませんでした (3002)` になる。

一方、**共有モードを明示する読み手なら開ける。**
VBA なら `Open path For Binary Access Read Shared As #n`。
実行中のログを読みたいときは ADODB を使ってはいけない。

## ★ 仕様事実 45 — `Debug.Print` は CP932 で表示するが、String 自体は無傷 ★

CP932 に無い文字を `Debug.Print` すると `?` になる。
**しかしそれは表示だけの話で、VBA の String は UTF-16 のまま無傷。**
UTF-8 でファイルに書けば正しく残る。

K-1 の実測では、イミディエイトに `サロゲートペア [??]` と出た同じ行が、
ログファイルでは `サロゲートペア [😀]` になっていた。

★イミディエイトの `?` を見て「データが壊れた」と早合点しないこと。★
仕様事実35 (ソースが CP932 に縛られる) とは別の話である。

## ★ 仕様事実 46 — VBE は `&H` リテラルの冗長な `&` サフィックスを落とす ★

`&H10000&` / `&H40000&` は import 後に `&H10000` / `&H40000` になる
(0x10000 以上は最初から Long なので `&` が要らない)。

一方 **`&HD800&` / `&HDC00&` の `&` は残る。**
0x8000〜0xFFFF は `&` が無いと Integer に化けるため、VBE も意味があると判断している。

★設計原則29 の危険域が「0x8000〜0xFFFF」であることの、VBE 自身による裏付け。★
import 後に `&` が消えていても破損ではない。

### K-2 以降への申し送り

- **残り 1,623 本の `Debug.Print` の移行。** 一度にやらない。
  次に効くのは `Wv2Pane` の `View_On*_Core` (イベントバーストでイミディエイトを
  埋めるのはここ) と `Wv2Browser`。
- **Sentinel MEM_FREE バグ (仕様事実10) の恒久対応** は K 軸の本丸。
  `Wv2Thunks.bas` = 全機能の土台で、しかも仕様事実20 によりデバッガが使えない領域。
  腰を据えられるときに。
- ログのローテーションは 20 本固定。困ったら定数 `LOG_KEEP_FILES` を変える。
- **性能は未計測。** 毎行フラッシュしていないので重くはないはずだが、
  D 軸の実テスト (数十往復) で体感を見てから判断する。
