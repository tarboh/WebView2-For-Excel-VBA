# VBA-WebView2 開発メモ 第3部 — D 軸 (DOM / ページ自動制御)

ロードマップ R-4 (MSHTML 相当) の実装記録。コンセプト①「Web ページを自動制御する
ことに優れた使いやすさ」への直接の投資にあたる。**現役の追記先。**

---

## D-1 段階 — ExecuteScript の同期取得プリミティブ (EvalSync)

### 位置づけ

ロードマップ R-4 (MSHTML 相当) の第一歩。**要素レジストリや Wv2Element はまだ作らず、
「非同期 API を同期の顔で使う経路が現行アーキテクチャの上で安全に成立するか」だけを
確かめる**ことに絞った段階。ここが通れば以降は案 C-1 の横展開に落ちる。

### 事前調査で分かったこと (思ったより軽かった)

- `View_ExecuteScript` は呼び出しごとに callback ID を採番し独立スロットを取る
  (第9.8c 案 a) = **並行呼び出しが既に安全**
- `View_ExecuteScriptSafe` が `{ok:true,value:…}` / `{ok:false,errorName:…}` を返す (第9.9c)
- `WaitPaneReady` の DoEvents ループが待ちの前例 (`READY_TIMEOUT = 10#`)
- 旧版 (`旧WebView2.cls`、通常サンク方式) に `ExecuteScriptSync` の前例があり、
  フラグ + DoEvents + タイムアウトという骨格は既に通っていた

→ D-1 は「非同期の仕組みを作る」ではなく **「callbackId と結果を突き合わせて待つ層を
1 枚被せる」** 作業だった。

### 論点と決定 (8 件を一括合意)

| # | 論点 | 決定 |
|---|---|---|
| 1 | メソッド名 | 接頭辞なしの `EvalSync`。`View_` / `Ctrl_` は COM のメソッドと 1 対 1 の意味なので、待ちループ内蔵の高レベル API には付けない |
| 2 | 結果の受け口 | callbackId をキーにした保留テーブル (設計原則87)。`UserForm1.OnExecuteScriptResult` 直呼びは Debug.Print だけなので残す |
| 3 | 戻り値の形 | `String` (= value の JSON 表現)。成否は `LastEvalOk` / `LastEvalError`。型変換は D-2 以降 |
| 4 | Safe 経由 | 常に `View_ExecuteScriptSafe` を通す。生だと JS 例外が S_OK + "null" で返り正常な null と区別できない |
| 5 | タイムアウト | 既定 5 秒。超過したら**破棄済みで残し**、遅延到着はそれを見て捨てる |
| 6 | 並行・ネスト | 許容。callbackId で分離されるので取り違えない。深さをログに出すだけ |
| 7 | in-callback ガード | ★本段階の目玉★ `m_inCallback > 0` なら JS を撃たずに即失敗 (設計原則88) |
| 8 | 検証 | `Test_D1_Eval` / `Test_D1_Guard` / `Test_D1_Help` |

### 追加した Public メンバ (Wv2Pane)

- `EvalSync(jsExpr, [timeoutSec=5]) As String`
- `LastEvalOk As Boolean` / `LastEvalError As String`
- `ResetCallbackGuard()` / `InCallbackDepth As Long`
- `Debug_SetInCallback(n)` (検証専用の注入口)

使用例:

```
?UserForm1.GetActivePane.EvalSync("document.title")                        → "Bぷよ"  (引用符付き)
?UserForm1.GetActivePane.EvalSync("document.querySelectorAll('a').length")  → 22
```

### 実装上の事後報告 (設計原則79)

1. **ハンドラを `_Core` に分離した。** 合意時は「各 View_On* の入口で +1、出口で -1」と
   説明したが、実コードは `View_OnNewWindowRequested` に途中 Exit Sub が 5 箇所、
   他も 1 箇所ずつあった。本体を `Private View_OnXxx_Core` にリネームし、同名の Public
   薄いラッパーが `+1 → Core → -1` を行う形にした (7 個)。→ 設計原則89 として一般化。
2. **エラー時もカウンタを戻すが握り潰さない。** `On Error GoTo GuardExit` で戻したあと
   `Err.Raise` で同じエラーを投げ直す。`ComCallbackHandler` にエラー処理は無く従来は
   そのまま伝播していたので、挙動を変えないため。
3. **`OnExecuteScriptCompleted` にはガードを付けていない。** 結果の受け口であり中で
   ユーザーコードが走らないため。付けると EvalSync の DoEvents 中にカウンタが立ち紛らわしい。
4. **JSON の取り出しは Wv2Pane 内の Private 関数 `JsonPickStr` にした。**
   `Wv2Json.JsonGetStr` は設計原則64 により**エスケープを解かない**実装で、二重 JSON
   エンコードされた値の中の最初のエスケープ引用符で切れてしまう。他所から要るように
   なったら Wv2Json へ昇格させる。
5. **`JSON.stringify` で包む形にした。**
   `View_ExecuteScriptSafe("JSON.stringify(" & jsExpr & ")")` を撃つと、Safe が返す
   value が**常に文字列**になり、限定パーサ 1 本で型を問わず取り出せる。

### 実装前に済ませた検査

- 整形: UTF-8 BOM なし / CRLF のみ / 二重 CR なし / 裸 LF ゼロ
- 構文: 引用符の偶奇、行継続 `_` の右のコメント、Sub 27・Function 46・Property 13 の開閉一致
- **`JsonPickStr` のアルゴリズムを Python で再現し実データで検証**:
  12 型 × 外側の非 ASCII を `\uXXXX` にする場合としない場合 = 24 ケース全通過。
  例外系の `"ok":false` 検出と errorName / errorMessage の取り出しも確認。
  ※ 最初シミュレータが全 NG になったが、原因は `json.dumps` が `": "` と空白を入れる
    ことで、JS の `JSON.stringify` は空白を入れない。**検査側の作り込み不足だった**。
- **予約語チェック (設計原則90)**: 49 個の識別子を照合。`eNum` の事故を受けて常設化。

### 実機検証の結果 (全合格)

- **型**: 整数 / 小数 / 文字列 / 日本語 / 引用符入り / バックスラッシュ入り / 真偽値 /
  null / undefined / オブジェクト / 配列 / 300 字 すべて正しく復号
  (`{"ok":true,"value":"{\"a\":1,\"b\":[1,2,3]}"}` → `{"a":1,"b":[1,2,3]}`)
- **実ページ**: `document.title` → `"Bぷよ"`、`location.href`、`document.body.innerText.length` → 305、
  `document.querySelectorAll('a').length` → 22
- **失敗系**: ReferenceError / Error: boom / syntax-error-or-null をそれぞれ正しく分類
- **タイムアウト**: cb19 を破棄済みにして 1 秒で復帰 → 遅延到着を捨てるログ →
  直後の cb20 が正常。**論点5 の設計が実測で裏付けられた**
- **ガード**: `Test_D1_Guard` の 2) で `View_ExecuteScriptSafe` のログが**一切出ない** =
  JS を発行する前に弾いている。固まらずに即戻る
- **回帰 (View_On* を 7 本包み直した影響)**: タブ追加 / 切替 / 閉じる / D&D 並べ替え /
  URL 入力 / 戻る / 進む / リロード / リンクの新タブ展開 / 設定タブの検索エンジン変更、
  すべて正常。**`Test_9_26_PostProbe` の POST リンクも、プリウォーム委譲・
  `put_NewWindow`・POST 本文・`Sec-Fetch-User: ?1`・Referer・Origin まで完全に元どおり**
  (仕様事実26 が再現)。ラッパー化は無害と確認できた。

### 踏んだ事故

**★予約語との衝突★** `Dim eNum As Long` がコンパイルエラー。VBA は識別子の大小を
区別しないので `eNum` = `Enum`。たーぼーさんの指摘で発覚した。→ 設計原則90 として
検査を常設化。

### 変更ファイル

`Wv2Pane.cls` (D-1 段階) / `Wv2Tests.bas` (D-1 段階) の 2 本のみ。

### 確立した設計原則 (86〜90)

- **設計原則86 (D-1) 段階番号は「軸の記号 + 連番」で振る。**
  一本の連番を伸ばし続けると、性質の違う仕事が同じ列に並んで後から読めなくなる。
  軸が変わったら番号を大きくするのではなく記号を分ける。過去の軸は凍結して書き換えない。

- **設計原則87 (D-1) 非同期 API を同期の顔で使うときは、結果を「単一変数」で受けない。**
  callbackId をキーにした保留テーブルで受ける。単一変数だと並行呼び出しで結果を
  取り違える。既に呼び出し側が並行安全に作られているのに (View_ExecuteScript は
  9.8c で callbackId 採番済み)、上位層でその性質を捨てるのは設計の後退である。
  ★タイムアウトしたエントリは削除せず「破棄済み」で残す★ こと。消すと遅れて来た
  完了通知が「知らない ID」になってログが濁り、実際に来たのかどうかも分からない。

- **設計原則88 (D-1) デバッガが使えない領域では「固まる」を「即座のエラー」に変える。**
  仕様事実 14 (イベントハンドラ内から同期待ちするとデッドロック) は、仕様事実 20 に
  よりブレークもステップも打てない領域で起きるので、踏むと原因究明が極端に苦しい。
  踏めないようにガードを置き、拒否理由をログに書き、復帰手段 (ResetCallbackGuard) も
  同時に用意する。ガードの存在自体がドキュメントになる。

- **設計原則89 (D-1) 途中に Exit のある既存プロシージャへ前後処理を足すときは、
  本体を `_Core` に分離して薄いラッパーで包む。**
  入口と出口に 2 行足す方式は、途中 Exit があると戻し漏れの経路を作る
  (View_OnNewWindowRequested は Exit Sub が 5 箇所あった)。ラッパーなら
  `On Error GoTo` で全経路を一点に集約でき、公開名も変わらないので呼び出し側は無改修。
  ★エラーは握り潰さず Err.Raise で投げ直す★ こと (従来の伝播挙動を変えない)。

- **設計原則90 (D-1) 新規識別子は VBA 予約語リストと「大小無視」で照合する。**
  VBA は識別子の大小を区別しないので、`eNum` は予約語 `Enum` そのものになり
  コンパイルエラーになる (D-1 で実際に踏んだ)。見た目が違っても衝突する。
  Python の検査工程に「`Dim x As` / `ByVal x As` / `Private x As` を正規表現で拾い、
  約 170 語の予約語集合と小文字化して突き合わせる」を常設した。

### 確立した仕様事実 (29〜32)

- **仕様事実29 (D-1) ExecuteScript の結果 JSON はキーがアルファベット順に並び替えられる。**
  View_ExecuteScriptSafe の JS は `{ok, errorName, errorMessage, stack}` の順で
  オブジェクトを作っているのに、VBA に届く時点では
  `{"errorMessage":...,"errorName":...,"ok":false,"stack":...}` になっている。
  ランタイムが結果を作り直しているということ。**位置に依存する解析を書くと壊れる。**
  キー名で探すこと。

- **仕様事実30 (D-1) 結果 JSON では `<` が `\u003C` にエスケープされて届く。**
  例外の stack に `at \u003Canonymous>:1:31` として実際に現れた。
  したがって JSON を自前で解くなら **`\uXXXX` の復号は保険ではなく必須**。
  D-2 で innerHTML を取ると全タグがこの形で来る。

- **仕様事実31 (D-1) JSON.stringify(undefined) は undefined を返すため、
  結果オブジェクトから value キーごと消える。**
  `{"ok":true}` だけが届く。これは「戻り値が undefined だった」の合図として使える
  (エラーではない)。EvalSync はこれを検出して文字列 undefined を返している。

- **仕様事実32 (D-1) JS がスレッドをブロックしている間、ExecuteScript の完了通知は
  届かず、ブロックが解けた瞬間にまとめて届く。**
  3 秒のビジーループを 1 秒でタイムアウトさせた検証で、打ち切り後に完了通知が
  遅れて到着した (cb19)。仕様事実 27 (hostObjects.sync 中は呼び出し元ペインの JS が
  完全にブロックされる) と同じ構造で、**待つ側は必ずタイムアウトと遅延到着の始末を
  持たなければならない。**
  ※ JS の構文エラーが errorCode=0 + resultJson="null" で返る件は第9.9c で確認済み。
    D-1 でも同じ挙動を再確認した (EvalSync は syntax-error-or-null として扱う)。

### D-2 以降への申し送り

- 文字列は**引用符付きで返る**。D-2 で `Wv2Element` のプロパティごとに型を決めて剥がす。
- `jsExpr` は「文」ではなく「式」。複数文は即時関数で包む (Safe ラッパーの制約を継承)。
- 要素レジストリ (`__wv2els[]`) は**ページ遷移で消える**ので、`Wv2Element` は
  「Pane 参照 + ハンドル + 世代番号」の 3 つを持たせて古い参照を検出できるようにする。
- `QuerySelectorAll` は JS 側で全ヒットを一括登録しハンドル配列を返せば**往復 1 回**で済む。
  戻り値は VBA 標準の `Collection`。`Wv2Elements` クラスは作らない。
- `Wv2Document` は作らない (Wv2Pane が既に title / URL / readyState / DOMContentLoaded を
  持っており、挟むと二重化するだけ)。検索の入口は `Wv2Pane` に直付け。
- ★`EvalSync` の DoEvents ループ中もブレーク/ステップ実行しないこと★ (仕様事実20 の追補)。
- ★ハンドラ内で JS の結果が要るときは SafeTimer で一拍置いてから呼ぶ★
  (`RequestNewTabAsync` と同じ型)。直呼びは in-callback ガードで即失敗する。

---

## D-2 段階 — 要素レジストリと Wv2Element の読み取り

### 位置づけ

D-1 で「非同期 API を同期の顔で使う」経路が実証されたので、その上に載る本番の層。
**要素を掴んで値を読む**ところまでを担当し、書き込み (`.Value =` / `.Click`) は D-3、
コレクション (`QuerySelectorAll`) と寿命管理は D-4 に送る。

### 論点と決定 (9 件を一括合意)

| # | 論点 | 決定 |
|---|---|---|
| 1 | JS 側レジストリの注入タイミング | **案c: 注入しない。** 検索式そのものが「無ければ自分で作る」形にする |
| 2 | 世代番号の発行元 | **案b: JS が発行** (`'g' + Date.now() + '-' + Math.random()`)。判定の権威は常に JS 側 |
| 3 | ハンドルの解放 | **案a: 解放しない。** `ClearElementRegistry` を手動掃除用に 1 本だけ |
| 4 | 見つからないときの返し方 | **案a: `Nothing`。** 成否は `LastEvalOk` で区別する |
| 5 | `Wv2Element` の公開メンバ | 読み取り 8 種 (下記)。書き込みは D-3 |
| 6 | 引用符剥がしの置き場所 | **案b: `Wv2Json.bas` に集約** |
| 7 | `QuerySelectorAll` を含めるか | **案a: 含めない (単数のみ)。** JS 側だけ複数対応の形にしておく |
| 8 | 検証ページ | **案b: 自前 HTML を `NavigateToString`** |
| 9 | 検証 Sub | `Test_D2_Find` / `Test_D2_Stale` / `Test_D2_Help` の 3 本 |

### ★論点1: 注入しない (self-bootstrapping)★

`AddScriptToExecuteOnDocumentCreated` (完了ハンドラ用の HandlerKind と vtable が新たに要る)
も、`DOMContentLoaded` での毎回注入 (ハンドラ内なので `EvalSync` が使えず、タブバー /
ナビバー / 設定タブの Pane にも撃たれる) も採らない。代わりに **D-2 の全 JS 式の先頭に
共通のプロローグを置く**。

```js
if(typeof window.__wv2==='undefined'||!window.__wv2){
  window.__wv2={gen:'g'+Date.now()+'-'+Math.random(),els:[]};}
var R=window.__wv2;
```

- 新しい COM 機構が要らない
- 往復が増えない (判定・生成・検索が 1 つの式に入る)
- ページ遷移 / SPA の書き換え / `about:blank` のどれで消えても、次のアクセスで
  自動的に作り直される (設計原則74 と同じ自己修復性)

**欠点**: ページ読み込み**前**にフックを仕込む用途には使えない。それが要るときは
`AddScriptToExecuteOnDocumentCreated` を独立段階で実装する。

### ★論点2: 世代は JS が発行する★

VBA 側でナビゲーションを数える案は、SPA の `pushState` では DOM が残るのにイベントは飛び、
逆にページ内 JS が要素を差し替えてもイベントは飛ばないので**実態とズレる**。
JS 側が発行すれば「レジストリが消える = 世代も消える = 作り直しで必ず別値」が保証される。

`Wv2Pane.m_domGen` は「JS から最後に聞いた世代」のキャッシュで、**高速パスにしか使わない**。
手元の世代とキャッシュが違えば往復せずに stale と確定でき、一致していても JS が作り直して
いるかもしれないので必ず問い合わせる。**キャッシュが古いときは必ず「問い合わせる」側に
倒れる**ので誤判定は起きない。

### 追加した Public メンバ

**`Wv2Pane`**

| メンバ | 内容 |
|---|---|
| `GetElementById(id)` / `QuerySelector(sel)` | `Wv2Element` を返す。無ければ `Nothing` |
| `ClearElementRegistry()` | レジストリを新しい世代で作り直す |
| `CurrentDomGen` | 世代キャッシュ (権威ではない) |
| `JsQuote(s)` *(Friend)* | VBA 文字列を JS の文字列リテラルにする |
| `Elem_Read` / `Elem_CheckStale` *(Friend)* | `Wv2Element` 専用の経路 |

**`Wv2Element` (新規クラス)**

`TagName` / `InnerText` / `InnerHTML` / `Value` / `GetAttribute(name)` / `IsStale` /
`Handle` / `Generation` / `LastOk` / `LastError`

**`Wv2Json`**

`JsonUnescapeAt(json, startPos, outVal)` / `JsonUnescape(raw)` / `JsonPickStr(json, key, outVal)`

### ★Nothing と空文字の読み方 (論点4 の規約)★

| 場面 | 判定 |
|---|---|
| `Nothing` + `Pane.LastEvalOk = True` | **本当に存在しない** (正常) |
| `Nothing` + `Pane.LastEvalOk = False` | **失敗**。理由は `Pane.LastEvalError` |
| 空文字 + `Element.LastOk = True` | **本当に空** (テキストが無い / 属性が無い) |
| 空文字 + `Element.LastOk = False` | **失敗**。理由は `Element.LastError` (`stale` / `no-pane` / `in-callback` / `timeout(n s)`) |

### 実装上の事後報告 (設計原則79)

- **(a) `JsonPickStr` を `Wv2Pane` から `Wv2Json` へ移設した (Private → Public)。**
  論点6 の合意は「`JsonUnescape` を新設し `JsonPickStr` もそれを呼ぶ形に書き換える」
  だったが、`Wv2Element` も同じ復号を要るため、`Wv2Pane` に Private で残すと 2 実装になる。
  走査部は `Wv2Json.JsonUnescapeAt` に同一移植し、ロジックは 1 文字も変えていない。
  既存の `JsonGetStr` (エスケープを解かない、設計原則64) は**一切変更していない**。
- **(b) `Wv2Element` に `LastOk` / `LastError` を足した (論点5 の 8 メンバ + 2)。**
  読み取り系は失敗時も空文字を返すため、「本当に空」と「stale」を区別する手段が要る。
  `Pane.LastEvalOk` は JS 実行自体の成否なので、stale のとき True になり代用できない。
- **(c) `Elem_Read` / `Elem_CheckStale` の戻り値を Enum でなく `Boolean` + 状態文字列にした。**
  クラスモジュールの `Public Enum` が他モジュールから無修飾で見えるかは本プロジェクトに
  前例がなく (`ControllerState` も `BrowserState` も定義元でしか使われていない)、
  コンパイルエラーになると 1 往復無駄になる。D-1 の流儀にも揃う。

### 実装前に済ませた検査

- **Python 先行検証 (34 ケース、NG 0)** — `JsonUnescape` を Python で再現し、
  `\u003C` / 引用符 / バックスラッシュ / 改行 / 制御文字 / **サロゲートペア** /
  `\uXXXX` 展開形で検証してから VBA に落とした。
  ★最初に絵文字 2 件が NG になったが、原因は**検査側の作り込み不足**★
  (Python の `str` 連結ではサロゲートの片割れが再結合しない)。D-1 と同じパターン。
- **JS を復元して node で実行 (11 ケース、NG 0)** — VBA が組み立てる JS 文字列を Python で
  再構成し、最小 DOM スタブと共に **node で実際に走らせて**挙動まで確認した。
  構文チェックだけでなく、レジストリ自己生成・`found` 判定・`has:false`・世代不一致・
  添字不在・掃除後の stale までを実行前に潰せた。**この手法は有効なので継続する。**
- 整形・予約語 (宣言識別子 341 種)・Public 名の重複 (15 名)・JS-in-VBA 規則
- **原典との全数照合** — `Wv2Pane` の消失 64 行がすべて移設した `JsonPickStr` と
  書き換えた呼び出し 3 行であることを確認し、意図しない欠落がゼロであることを保証した。

### 実機検証の結果 (全合格)

`Test_D2_Find` / `Test_D2_Stale` / 回帰の `Test_D1_Eval` / `Test_D1_Guard` すべて
表示範囲で `[OK  ]`、`in-callback` 深さも 0。タブ操作・設定タブも正常。

★ログが流れて前半が読めなかったが、**ハンドルの連番で健全性が確定した**★
`(5)` の冒頭が `handle=9` で、`(1)`〜`(4)` が取る要素はちょうど 9 個。1 つでも取得に
失敗していれば `D2El` がダミーを返してハンドルが 1 つずれるので、この一致は偶然では起きない。

**論点2 が実機で証明された。** 遷移前 `g1787142010489-0.8716307937600973` →
遷移後 `g1787142011015-0.07404330783805646`。VBA は何もしていないのに、JS 側の
レジストリが消えて作り直された結果として世代が変わり `s:"stale"` が返った。

### 確立した設計原則 (91〜93)

- **設計原則91 (D-2) 状態の正当性判定は、その状態を実際に保持している側で行う。**
  手元のキャッシュは「**安全側にしか外さない**高速パス」にしか使わない。
  世代の照合を JS 側で行い、`m_domGen` は往復を省くためだけに使うのがこの形。
  キャッシュが古い場合に「問い合わせる」側へ倒れることを必ず確認すること。

- **設計原則92 (D-2) 実行環境が消える場所に置く仕組みは、注入するのではなく
  「使う式が自分で用意する」形にして自己修復させる。**
  注入タイミングを管理し始めると、遷移・SPA 書き換え・`about:blank` の全経路を
  こちらで把握する羽目になる。プロローグを毎回の式に埋め込めば管理そのものが消える。
  設計原則74 (全体同期は取りこぼしに対する自己修復性を持つ) と同じ発想。

- **設計原則93 (D-2) 失敗時も正常時と同じ型を返す API には、成否を問い合わせる口を
  必ず対で用意する。**
  空文字を返す読み取り API は「本当に空」と「失敗」が区別できない。D-1 の
  `LastEvalOk` / `LastEvalError` と同じ対を `Wv2Element` にも置いた。
  **設計時にメンバ数を数えて満足せず、「この戻り値だけで呼び出し側が判断できるか」を
  必ず問うこと。**

### 確立した仕様事実 (33〜34)

- **仕様事実33 (D-2) 結果 JSON では `<` だけが `\u003C` にエスケープされ、`>` は
  そのまま届く。**
  D-2 の実機ログで `at \u003Canonymous>` という非対称な形が観測された。
  仕様事実30 の正確な形はこちら。復号はどちらでも通るので実害はないが、
  シミュレータを書くときは実物に合わせること。

- **仕様事実34 (D-2) VBA の String は UTF-16 コードユニット列なので、サロゲートペアは
  片割れずつ `ChrW$` で足せば正しく復元される。**
  `CLng("&HD83D")` は 16 ビット符号付きとして -10179 になるが、`ChrW$` は負値を
  65536 + n と解釈するので D83D の文字が得られ、次の DE00 と連結すればペアになる。
  ★Python でシミュレートするときは注意★ Python の `str` 連結では再結合しないため、
  コードユニット列として持って最後に一括デコードしないと**偽の NG** が出る。

### 変更ファイル

`Wv2Element.cls` (**新規**) / `Wv2Pane.cls` / `Wv2Json.bas` / `Wv2Tests.bas` の 4 本。
`Wv2Browser.cls` は無変更 (`ActivePane` / `AddTabWithHtml` が既にあった)。

### D-3 以降への申し送り

- **読み取りは 1 プロパティにつき 1 往復する。** ループで大量に読むと遅いので、
  まとめて取りたい場合は `EvalSync` で JS 式を 1 本書いた方が速い。D-2 の割り切り。
- `QuerySelectorAll` は JS 側の登録を `push` からループに変えるだけで足せる形にしてある。
- **detached な要素は stale にならない。** 世代が同じで配列に残っていれば読める。
  DOM から外れたかを見たいなら `document.contains(e)` を別途足すことになる。
- D-4 (寿命管理) では、同一ページ内で検索を繰り返すと `els` が伸び続ける件を扱う。
  ★`Class_Terminate` からの解放は採らない★ (Terminate の文脈を制御できず、
  in-callback ガードや `Ctrl_Close` 済みの Pane を踏みやすい)。
---

## D-3 段階 ― 書き込みと操作 + DOM 条件待ち

### 位置づけ

D-2 で「今の DOM を読む」手段が揃ったので、その裏返しの**書く・操作する**と、
`.Click` を入れた瞬間に必ず要る**変化を待つ**をまとめて担当する。
コンセプト① (Web ページを自動制御することに優れた使いやすさ) への直接の投資。
静穏待ち (MutationObserver / network idle) と `QuerySelectorAll` / 寿命管理は D-4 へ送る。

### 論点と決定 (8 件を一括合意)

| # | 論点 | 決定 |
|---|---|---|
| 1 | この回のスコープ | **案A: 書き込み・操作 + DOM 条件待ち。** 待ちを次段に送ると自動化が使えない |
| 2 | 待ちの形 | **案C: 明示待ちを第一級、暗黙待ちは opt-in** (`ImplicitWaitSec` の既定は 0) |
| 3 | 待ちループの場所 | **VBA 側でポーリング** (未知1 の実測で確定。下記) |
| 4 | ポーリング間隔 | **案A: 固定 100ms** |
| 5 | 待つ条件 | **案A: 存在だけを見る。** 可視性は別 API (`WaitVisible`) に分ける |
| 6 | `.Value =` の伝え方 | **ネイティブ setter → `input` → `change`。両方撃つ** |
| 7 | `.Click` の形 | **案A: `e.click()` 優先、無ければ `MouseEvent` を dispatch** |
| 8 | 戻り値の規約 | D-2 の `LastOk` / `LastError` を踏襲。`Value` は Property Let のまま |

### ★着手の初手で前提の誤りを見つけた★

論点の下書きにあった `Test_D3_Probe_Promise` は `EvalSync` で Promise を撃つ形だったが、
**これでは未知1 (ExecuteScript は Promise を待つか) を測れない。**

`EvalSync` は `View_ExecuteScriptSafe("JSON.stringify(" & 式 & ")")` を撃つ。つまり
**式が Promise を返しても、その場で `JSON.stringify` されて `{}` に潰れる。**
ExecuteScript に届く時点で戻り値はもう Promise ではないので、待つ性質があっても観測できない。

→ 実測を 2 段構えに組み替えた (製品コードは 1 行も足していない):

| | 何を見るか | 判定 |
|---|---|---|
| (A) `EvalSync` 経由 | Promise が `{}` に潰れること | 自動判定 |
| (B) `View_ExecuteScript` 直呼び | `OnExecuteScriptCompleted` が出す `resultJson=` の行 | ★目で見る★ |

(B) の結果は `EvalSync` の保留テーブルを通らないので VBA からは受け取れない。
そこで `★★★ ここから / ここまで ★★★` のマーカーで挟み、**その間に** `resultJson=` が
出たかどうかで読む形にした。

### ★未知1 の実測結果: ExecuteScript は Promise を待たない★

```
(B-1) Promise.resolve(42)         → resultJson={}  resultLen=2
(B-2) new Promise(function(r){})  → resultJson={}  resultLen=2   ← ★決定打★
```

**決して解決しない Promise** を渡しても即座に完了通知が来た。待つ実装ならここは
無言のままになるので、これで確定 (仕様事実49)。よって論点3 は下書きの骨格どおり
**VBA 側のポーリング**で実装した。

★仮に「待つ」だったとしても、使うには D-1 の包み方 (`JSON.stringify` を先に噛ませる形)
を作り直す必要があった★ ので、「JS 側で待つ」案の値段は論点3 の見積もりより高かった。

### 追加した Public メンバ

**Wv2Element (書き込みと操作)**

| メンバ | 形 | 意味 |
|---|---|---|
| `Value = v` | Property Let | 値を書き込む。★戻り値が無いので成否は `LastOk`★ |
| `Click()` | `Boolean` | クリックする |
| `SetAttribute(n, v)` | `Boolean` | 属性を書き込む |
| `LastInfo` | `String` | 直前の操作が選んだ経路 (`setter` / `direct` / `click` / `dispatch` / `ok`) |

**Wv2Pane (DOM 条件待ち)**

| メンバ | 形 | 意味 |
|---|---|---|
| `WaitFor(selector, timeoutSec)` | `Wv2Element` | 現れるまで待つ (既定 10 秒) |
| `WaitGone(selector, timeoutSec)` | `Boolean` | 消えるまで待つ (既定 10 秒) |
| `ImplicitWaitSec` | `Single` | ★既定 0 = 無効★ の暗黙待ち (opt-in) |

内部は `Elem_Exec` (Friend、`Elem_Read` と対の「撃つ側」) / `FindWithWait` /
`ExistsNow` / `PollPause` / `ElapsedSec` の 5 本。

### ★成功の意味 (D-3 で最も誤解しやすいところ)★

**`True` は「JS が例外なく走った」であって「意図した効果が出た」ではない。**
disabled なボタンを `Click` しても例外は出ないので `True` が返る。readonly な input への
書き込みも同じ。`div` に `.Value =` すると独自プロパティが 1 個生えるだけだが、
やはり `LastOk = True` になる (経路は `direct`)。**効果が要るときは読み戻して確かめる。**

### ★論点6: `.Value =` がやっていること (SPA の核心)★

素直に `e.value = x` とすると、React / Vue は値の変更に**気づかない**。
フレームワークは要素インスタンスに独自の value setter (value tracker) を被せていて、
そこを通った代入は「自分が知っている値」として記録されてしまうため。

```js
var d = Object.getOwnPropertyDescriptor(e.constructor.prototype, 'value');
if (d && d.set) { d.set.call(e, v); } else { e.value = v; }
e.dispatchEvent(new Event('input',  { bubbles: true }));
e.dispatchEvent(new Event('change', { bubbles: true }));
```

`e.constructor.prototype` から取れるのは**ネイティブの** descriptor なので、
インスタンス側の tracker を迂回できる (仕様事実50)。`input` は React 系、
`change` は jQuery 系が見るので**両方撃つ**。素の HTML では `change` が 1 回
余分に飛ぶが、「反応しないページがある」事故の方が高くつくと判断した。

### ★待ちの設計★

**戻り値の意味は D-2 の規約 (設計原則93) をそのまま踏襲する。**

| | `LastEvalOk = True` | `LastEvalOk = False` |
|---|---|---|
| `WaitFor` が `Nothing` | 時間内に現れなかった (正常) | 失敗 (セレクタ不正 / in-callback) |
| `WaitGone` が `False` | 時間内に消えなかった (正常) | 失敗 |

★**失敗と判った時点で待たずに抜ける**★ (設計原則104)。セレクタの構文エラーや
in-callback (仕様事実14) は待っても直らない。粘ると、イベントハンドラ内から呼んだときに
指定秒ぶん Excel が固まる。実機では 5 秒指定の不正セレクタが **0.05 秒**で返っている。

**暗黙待ちを既定にしない理由** (論点2): 既定で待つと「本当に無い」の確認が毎回
タイムアウト待ちになり、D-2 で決めた「`Nothing` + `LastEvalOk=True` = 本当に存在しない」
の意味が濁る。Selenium 流にしたい人だけが `ImplicitWaitSec` を上げる。

### 実装上の事後報告 (設計原則79)

1. ★**`Wv2Element.LastInfo` を足した**★ (論点8 の 3 メンバ + 1)
   `.Value =` が**ネイティブ setter 経由 (`setter`) か素の代入 (`direct`) か**は、
   SPA に効いたかどうかの唯一の手掛かりで、これが無いと未知3 を検証できない。
   読み取り系を呼ぶと空文字に戻る「直前の操作」の記録で、成否の判定には使わない。

2. ★**`WaitGone` はレジストリに積まない専用の存在確認 (`ExistsNow`) を使う**★
   `FindOne` を回すと、まだ消えていない要素をポーリングのたびに `R.els` へ push して
   しまう (10 秒待てば 70 個)。掴む必要が無いので `{g:R.gen,found:!!e}` だけを返す
   式を別に用意した。

3. ★**`Timer` の日跨ぎを補正した (`ElapsedSec`)**★
   `Timer` は深夜 0 時に 0 へ戻るので、素の引き算だと負になり**待ちループが永久に
   抜けなくなる** (仕様事実53)。またいだ分 (86400) を足して補正する。
   実機検証が 1:53 に走っている以上、他人事ではない。

4. **ポーリング中は「見つからない」ログを黙らせた** (`FindOne` に `quiet` を追加)。
   1 往復ごとに出るとイミディエイトが埋まる。`ImplicitWaitSec` を設定しない通常の
   `QuerySelector` は D-2 と同じく出る。

5. **`QuerySelector` / `GetElementById` を `FindWithWait` 経由に変えた。**
   ただし `ImplicitWaitSec` の既定が 0 なので、設定しない限り D-2 と同じ挙動 (1 往復で答える)。

### 実装前に済ませた検査 ― ★VBA ソースから JS を組み立て直して実 DOM で走らせた★

この PC に node は無かった (cscript の JScript は ES3 相当で `Object.defineProperty` も
`JSON` も無く、DOM も無いので代用にならない)。代わりに **Edge を headless で走らせた**
(`msedge --headless=new --dump-dom`。WebView2 と同じ Chromium)。

ハーネスの作り: **`src/*.cls` の連結式そのものを読み取って JS に展開する**
ミニ評価器 (文字列リテラル / 変数 / `JsQuote` `CStr` `JsRegistryPrologue` の呼び出しに対応)
を書き、`View_ExecuteScriptSafe` + `EvalSync` の包みまで再現した。ページ側で
`JSON.stringify(eval(wrapped))` した文字列は **VBA が受け取る `resultJson` そのもの**なので、
`JsonPickStr` 相当を Python で再現して端から端まで突き合わせられる。
検証ページ (`BuildD3ProbeHtml`) も VBA から取り出したので、**実装と検証がズレようがない**。

30 件超すべて OK。内訳は setter 経路 / 記号・引用符・バックスラッシュの往復 /
`input`+`change` が各 3 回 / ★React 風 tracker が素の代入では気づかず D-3 の書き方では
気づくこと★ / `click` と `dispatch` の両経路 / `isTrusted=false` / 属性の往復 /
不正な属性名が `InvalidCharacterError` で False / div への書き込みが `direct` /
select・textarea / レジストリ作り直し後が stale。

★**D-3b (待ち) のときはこのハーネスが使えなかった**★
たーぼーさんの Edge が起動していると `msedge.exe` はコマンドラインを既存インスタンスへ
渡して即座に終了する (仕様事実55)。`ExistsNow` の JS は組み立てて目視 + 括弧・引用符の
対応検査に留め、意味の検証は実機の `Test_D3_Wait` に任せた。

### 実機検証の結果 (全合格)

| テスト | 結果 |
|---|---|
| `Test_D3_Probe_Promise` | 未知1 が決着 (上記) |
| `Test_D3_Write` | ★33 件 OK / FAIL 0★ |
| `Test_D3_Framework` | ★7 件 OK★ (notified 0 → 1、trackedSet は 1 のまま) |
| `Test_D3_Wait` | ★29 件 OK / FAIL 0★ |
| 回帰 `Test_D2_Find` / `_Stale` / `Test_D1_Eval` / `_Guard` | ★31 / 8 / 20 / 3 件すべて OK★ |

**待ち時間の実測がきれいに揃った** (仕掛けは 800ms の `setTimeout`):

| 場面 | 上限 | 実測 | 読み |
|---|---|---|---|
| 既にある要素 | ― | **0.04 秒** | 待たずに返る |
| 遅れて現れる | 5 秒 | **0.89 秒** | 遅れは 90ms ≒ ポーリング 1 周期 |
| 現れない | 1 秒 | **1.02 秒** | 指定どおりで止まる |
| ★不正なセレクタ★ | 5 秒 | **0.05 秒** | ★待たずに諦めた★ |
| `WaitGone` | 5 秒 | **0.88 秒** | 同上 |
| `ImplicitWaitSec=5` | 5 秒 | **0.88 秒** | opt-in が効いている |

`Test_D3_Wait` の (2) では**待って掴んだ要素にそのまま書き込めている**。
待ち (D-3b) と書き込み (D-3a) が繋がっている証拠で、
`p.WaitFor("#result", 5).Value = "..."` のような使い方が実際に通る。

### ★検証の出し先を変えた (K-1 のログへ)★

`Test_D3_Write` の判定 40 件超がイミディエイトで読めなかった。`EvalSync` 1 往復ごとに
配管ログが 15 行出るので、600 行以上流れてバッファから溢れる。

そこで **D-1 / D-2 / D-3 の全テストの判定を `Wv2Log` に出す**ようにした。
`Wv2Log` は内部で `Debug.Print` も撃つのでイミディエイトの見え方は変わらない。
各テストの最後に集計を 1 行出す:

```
★判定 33 件: OK 33 / FAIL 0★
```

判定ヘルパーも 1 組に統合した (`D2Bool` / `D2Eq` と D-3 版が同じものだったので
`TestBool` / `TestEq` へ)。これで**ログファイルを検索するだけで合否が分かる**
(設計原則105)。`Test_D2_Find` は途中で `Exit Sub` する経路にも集計を入れてある。

### 手順書の抜け (★実機で発覚★)

`Test_D*_Help` の準備手順に **`UserForm1.Show vbModeless` が抜けていた**。
`hWnd_Frame` は `Frame1.[_GethWnd]` で取るので、フォームのウィンドウが実在しないと
0 のままで `Browser.Init` が失敗する (仕様事実54)。たーぼーさんが習慣で補完していたので
事故にはならなかった。D-1 / D-2 / D-3 の 3 つの Help を直した。

### 確立した設計原則 (103〜105)

- **設計原則103 (D-3) 測定の前に、測りたい性質が測定経路で保存されるかを確かめる。**
  「ExecuteScript は Promise を待つか」を `EvalSync` で測ろうとしたが、`EvalSync` は
  `JSON.stringify` を先に噛ませるので、Promise は経路の途中で `{}` に潰れていた。
  プローブが常に同じ答えを返す形になっていないか、**プローブ自体を検算する**こと。

- **設計原則104 (D-3) 待つ API は「まだ」と「失敗」を区別し、失敗と分かった時点で
  待つのをやめる。**
  リトライは「時間が経てば直る」ことに賭ける仕組みなので、時間で直らないもの
  (構文エラー / 権限 / in-callback) に適用してはいけない。ここを分けないと、
  誤った呼び出しほど長く固まるという最悪の挙動になる。

- **設計原則105 (D-3) 検証の判定は、流れて消える場所ではなく後から検索できる場所に出す。**
  イミディエイトウィンドウは配管ログで容易に溢れる。判定が読めなければ検証していないのと
  同じ。**出し先を変えるだけで、検証の再実行コストが「もう一度全部走らせる」から
  「ファイルを開く」に変わる。**

### 確立した仕様事実 (49〜55)

- **仕様事実49 (D-3) `ExecuteScript` は Promise を待たない。**
  戻り値が Promise でも `{}` として**即座に**完了通知が返る。決して解決しない Promise
  で確認済み。したがって「JS 側で待って結果を返す」形は書けず、待ちは VBA 側の
  ポーリングになる。

- **仕様事実50 (D-3) React などの value tracker は要素インスタンス側に `value` を
  被せるので、`prototype` のネイティブ setter を `call` しないと変更に気づかれない。**
  `e.value = x` は tracker を通り、tracker が「自分が書いた値」として記録するため、
  直後に `input` を撃っても差分が無いと判断される。
  `Object.getOwnPropertyDescriptor(e.constructor.prototype,'value').set.call(e, x)` なら
  tracker を迂回でき、`input` で差分が見える。**実機で notified 0 → 1 を確認済み。**

- **仕様事実51 (D-3) JS から dispatch した合成イベントの `isTrusted` は false。**
  `e.click()` でも `new MouseEvent('click')` でも同じ。`isTrusted` を見て弾くページには
  原理的に効かない。実機の観測値は `click/true/false` (type/bubbles/isTrusted)。

- **仕様事実52 (D-3) `EvalSync` 1 往復は実測 約 38ms。**
  33 件・約 40 往復を 1.41 秒。論点4 の見積もり (数 ms) より 1 桁遅い。
  100ms 間隔でポーリングすると**実効の判定間隔は約 140ms** になる。

- **仕様事実53 (D-3) `Timer` は深夜 0 時に 0 へ戻る。**
  経過時間を `Timer - t0` で測る待ちループは、日を跨ぐと負になって**永久に抜けなくなる**。
  負なら 86400 を足して補正する。

- **仕様事実54 (D-3) UserForm 上のコントロールの HWND は `Show` 後でないと取れない。**
  `Frame1.[_GethWnd]` は 0 を返し、`hWnd_Frame` が 0 のまま `Browser.Init` が失敗する。
  `StartWebView2_Full` は `UserForm1.Show vbModeless` が前提。

- **仕様事実55 (D-3) Edge が起動していると `msedge.exe` はコマンドラインを既存
  インスタンスへ渡して即座に終了する。**
  `--user-data-dir` を分けても効かない。症状は「終了コード 0・標準出力が空」で、
  `--headless --dump-dom` による検証ができなくなる。**見分け方は `msedge --version`**
  (何も出なければハンドオフされている)。`msedgewebview2.exe` も同じ。

### 変更ファイル

`Wv2Pane.cls` / `Wv2Element.cls` / `Wv2Tests.bas` の 3 本。新規モジュールは無い。
`Wv2Browser.cls` / `Wv2Json.bas` は無変更。

### D-4 以降への申し送り

- **静穏待ち (MutationObserver + fetch/XHR カウンタ) が D-4 の本命。**
  D-3 で待てるのは「DOM の条件」まで。「通信が全部終わって静かになるまで」は待てない。
  ★`fetch` / `XHR` のラップは設計原則92 (注入しない) との整合を先に決めること★
  自己生成プロローグでカウンタを毎回作り直す形にできるかが鍵。
- **`WaitVisible` (可視まで待つ) は未実装。** 論点5 で「存在だけ見る」に倒したので、
  必要になったら別 API として足す (`offsetParent !== null` など)。
- **`ImplicitWaitSec` は `WaitFor` / `WaitGone` には効かない。** 明示待ちが二重に
  粘るのを防ぐため。この非対称は意図的なので、変えるなら理由ごと変えること。
- **ポーリング中は長い `DoEvents` 区間になる** (未知2 のとおり)。10 秒待てば 10 秒間
  ずっと仕様事実20 の危険域にいる。`Test_D3_Wait` のヘルプにも書いた。
- `Wv2Element` はまだ**単数**のみ。`QuerySelectorAll` と寿命管理は D-4 のまま。

---
## D-4 段階 ― 静穏待ち (SPA の「落ち着いた」を判定する)

### 位置づけ

D-3 で入ったのは「**何を待つか書ける場合**の待ち」(`WaitFor` / `WaitGone`)。
外部からの提言が指摘した本題 ― **待つ対象が特定できないとき**にどうするか ― は
手つかずだった。D-4 はそこを埋める。

**a〜f の 6 段に刻んで、それぞれ独立に実機検証した。**

| 段 | 内容 | 実機 |
|---|---|---|
| D-4a | ページ内プローブ + 健康診断 | 28 件 OK |
| D-4b | `WaitSettled` + ノイズ除外 + 自動待ち | 22 件 OK |
| D-4c | 明示シグナル (`Arm*`) | 31 件 OK |
| D-4d | ★Google Maps の偵察★ (観測のみ) | 構造判定 5 件 OK |
| D-4e | 診断ログ / in-flight 台帳 / URL シグナル | 21 件 OK |
| D-4f | 居座る要求の除外 (`StaleInflightMs`) | 上記に含む |

### 論点と決定 (10 件を一括合意)

| # | 論点 | 決定 |
|---|---|---|
| 1 | スコープ | 静穏待ち一本 (`QuerySelectorAll` / 寿命管理は D-5) |
| 2 | プローブの入れ方 | **自己生成プロローグの拡張** (設計原則92 を維持) |
| 3 | 健康診断 | **入れる** |
| 4 | 判定式と既定値 | `inflight=0` かつ静穏 500ms / 上限 10 秒 / ポーリング 100ms |
| 5 | ノイズ除外 | 入れる (URL パターン + セレクタ)。遮断はやらない |
| 6 | 明示シグナル | `ArmContentSignal` / `ArmNetworkSignal`。ワンショット |
| 7 | 待ちと操作の結合 | **`WaitSettled` が第一級 + `AutoWaitAfterAction` は既定 False** |
| 8 | 戻り値と命名 | D-3 の規約踏襲。`WaitIdle` でなく **`WaitSettled`** |
| 9 | 診断ログ | 最小形で入れる |
| 10 | 検証 | 自前 HTML + ★実サイトは Google Maps★ |

### 追加した Public メンバ

| メンバ | 意味 |
|---|---|
| `WaitSettled(timeoutSec, stableMs)` | 通信と DOM が落ち着くまで待つ |
| `LastSettleInfo` | 待ちの内訳 (`stable q=611ms(eff=611) slack=111ms polls=8 ...`) |
| `AutoWaitAfterAction` | ★既定 False★ 操作の後に自動で待つ |
| `AddIgnoreNetwork` / `ClearIgnoreNetwork` / `IgnoreSelectors` | 静穏判定からの除外 |
| `ArmContentSignal` / `ArmNetworkSignal` / `ArmUrlSignal` / `DisarmSignals` | 明示シグナル |
| `StaleInflightMs` | ★既定 10 秒★ 居座る要求を数えない境目 |
| `SpaProbeState` / `SpaProbeHealthy` / `SpaProbeReset` | プローブの状態 |
| `SpaProbeLogging` / `SpaProbeDrainLog` | 診断ログ |

### ★プローブ (window.__wv2p) の作り★

`fetch` と `XMLHttpRequest.prototype.open/send` をラップして in-flight を数え、
`MutationObserver` で最後の DOM 変化時刻を持つ。**注入はしない** (設計原則92)。
呼ばれた式が自分で用意し、★呼ぶたびに健康診断して壊れていれば作り直す★。

健康診断が見るもの: 版番号 / `window.fetch === p.fw` / `XHR.open===p.xo` かつ
`send===p.xs` / `MutationObserver` の生存。1 つでも欠ければ**現在の `window.fetch` を
包み直して**作り直す (フレームワークが被せたラッパーごと包むので相手の機能を壊さない)。

★プローブを置く前に始まった通信は数えられない★ これは仕様。D-4c の
「操作の直前に arm する」運用と組み合わせれば実害が無い。

### ★静穏の判定式★

```
静穏 = (居座りを除いた in-flight が 0) かつ (最後の DOM 変化から stableMs 以上)
既定: stableMs=500 / 上限 10 秒 / ポーリング 100ms / 居座り境目 10 秒
```

★静穏窓は「`WaitSettled` を呼んだ時点」から測る★ (D-4b の事後報告)。
呼ぶ前から静かでもいきなり成立させない。さもないと**「まだ始まっていない」と
「もう終わった」を区別できない** (操作の効果が数十 ms 遅れて始まるのは普通)。
したがって★最短でも stableMs は見張る★。ただし stableMs より後に始まる処理は
原理的に取り逃す ― そこは明示シグナルの担当。

### ★★ WaitSettled が True でも「終わった」証明ではない ★★

**静かなだけ**である。アプリが要求を無視して静かなのか、終わって静かなのかは
区別できない。だから名前も `WaitIdle` ではなく `WaitSettled` にした (論点8)。
重要な操作では**明示シグナルを操作の直前に arm する**こと。

### 明示シグナル (arm してから act する)

| API | 何を待つか |
|---|---|
| `ArmContentSignal(selector)` | ★既存の★部分木が書き変わる。**arm 時に無ければ即 False** |
| `ArmNetworkSignal(pattern)` | URL 部分一致の要求が完了する |
| `ArmUrlSignal(fragment)` | ★URL がその文字列を含むようになる★ (pushState 対応) |

- **ワンショット**。次の `WaitSettled` 1 回で消費される (成功でも時間切れでも)
- **当たったら静穏窓を測り直す** (証拠の後にもう一度まるごと取る)
- ★**arm が消えたら黙って直さない**★ 遷移でプローブごと消えたら `signal-lost` で
  即失敗。そのまま待つと「条件なしの待ち」に化けて、静かになった瞬間に成功を返す
- ★**除外した要求でも明示シグナルの判定はする**★ 除外は「静穏判定から外す」
  だけの意味であって「見なかったことにする」ではない

### ★D-4d: Google Maps の偵察で分かったこと★

**実サイトは Google Maps、狙いは住所検索** (たーぼーさんの業務での実用が動機)。

#### 住所検索は通った ― 座標まで取れた

```
.Value = "東京都千代田区丸の内1-9-1" (経路=setter) → button[aria-label='検索'] を click
→ title = 〒100-0005 東京都千代田区丸の内１丁目９−１ - Google マップ
→ url   = .../place/.../@35.6824246,139.7682844,17z/...
```

★35.6824246, 139.7682844 = 東京駅★ D-3 の書き込み・クリックと D-4 の待ちが
実サイトで噛み合った。**URL に座標が入るので、完了の目印と欲しい値が同じもの**。

#### 予想が外れた ― Maps は静かだった

何もしない 15 秒で **DOM 変化 1 回・通信 1 回**。地図は canvas / WebGL で描かれるので、
タイル取得もアニメーションもプローブに映らない。「静まらないから除外必須」という
見立ては外れた。検索直後は `m=36 → 198 → 202` と動き**約 2 秒で収束**する。

#### ★本命の問題: 居座るストリーミング要求★

```
timeout inflight=1 q=11399ms polls=142 signal=url:hit
| 実行中: xhr 20060ms /search?tbm=map&authuser=0&hl=ja&gl=jp&pb=!4m12!...
```

URL シグナルは当たり、DOM も 11 秒静かなのに、**検索結果を流し続ける XHR が
1 本開きっぱなし**で `inflight=0` にならない。参考プロダクトが警告していた
「永久に idle にならないページ」の実例。

★これを突き止められたのは in-flight 台帳 (D-4e) を入れたから★
それまでは「なぜか静穏が来ない」としか分からなかった。

#### 対策は 2 段構え (D-4f)

| | 手段 | Maps での実測 |
|---|---|---|
| 汎用 | `StaleInflightMs` (既定 10 秒) | `WaitSettled` が **10.05 秒**で成功 |
| 正確 | `AddIgnoreNetwork "/search?tbm=map"` | **0.61 秒**で成功 |

**既定を汎用側に倒した**ので、知らないサイトでもとりあえず動く。速さと正確さが
要るときは名指しで外す。★捨てているもの★: 本当に遅いだけの要求も境目を超えれば
数えなくなる。ただしそのとき DOM も静穏窓のあいだ止まっているので、
「見た目は落ち着いている」という `WaitSettled` の意味とは矛盾しない。

#### ★診断ログが因果関係を見せた★

```
19583 DOM  x2                              ← 住所を書き込んだ
19585 NET+ /s?tbm=map&suggest=p&q=東京...   ← サジェスト要求
19690 NET- 105ms                           ← 応答
19700 DOM  x357                            ← サジェストの描画
19723 NET+ /search?tbm=map&pb=...           ← ★検索本体 = 居座る★
20250 NET+ /maps/_/MapsWizUi/data/batchexecute?rpcids=hspqX
20346 NET- 95ms
```

参考プロダクトが Google Flights で `rpcids=tDoGIe` を arm していたのと**同じ構造**が
我々のログにも出た。`batchexecute?rpcids=...` を arm すれば「検索が返った瞬間」を
正確に掴める。★何を待つべきかを推測でなく観測で決められるようになった★

### ★セレクタは推測で書かない★ (D-4d の事故)

最初 `#searchboxinput` で 30 秒待って見つからなかった。その場で DOM ダンプ道具
(`Test_D4_Dom`) を作って覗いたところ:

```
INPUT id=ucc-1 name=q type=text cls=UGojuc ...   ← ★id は自動生成★
BUTTON aria=検索 type=submit cls=mL3xi
```

**かつての id は消え、今は `ucc-1`**。`name='q'` と `aria-label='検索'` は残っていた。
候補を順に試して★どれが当たったかをログに残す★ 形に変えた (`D4FindFirst`)。
`Test_D4_Dom` は今後どのサイトを自動化するときにも使える道具として残る。

### 実装上の事後報告 (設計原則79)

1. ★**静穏窓を「呼び出し時点」から測るようにした**★ (上記)
2. ★**除外リストは空でも必ず送る**★ 「空だから送らない」にすると**ページ側に
   前の設定が残り続け**、`IgnoreSelectors = ""` や `ClearIgnoreNetwork` が効かない。
   実機検証の FAIL 1 件がこれだった
3. **ポーリング中の「見つからない」ログを黙らせた** (`FindOne` に `quiet`)
4. ★**`D4WaitPane` を検証側に足した**★ タブを開いた直後は `EvalSync` が `no-view` で
   失敗し、`WaitFor` / `WaitSettled` は「失敗は待たない」方針 (設計原則104) により
   即座に諦める。実サイトを開く回は先に Pane の準備を待つこと (`WaitFor` の
   コメントにも注意書きを入れた)
5. **`Test_D4_Dom` (DOM ダンプ) を新設した** 論点に無かったが、セレクタが外れた
   時点で「推測で直す」以外の道が無くなるため
6. ★**`import.ps1` に読み取り専用ガードを足した**★ (E 軸の改善。下記)

### ★ツールの穴を 1 つ塞いだ (E 軸)★

Excel がブックを開いていると `Workbooks.Open` が**読み取り専用で成功**する。
すると差し替えも事後照合も「メモリ上の複製」に対して成功し、
★ディスクには何も届かないのに「書き戻した (照合 OK)」と出る★。
D-4b の書き戻しで実際に踏んだ。`import.ps1` が `-Apply` 時に `wb.ReadOnly` を見て
中止するようにした。

### 実装前に済ませた検査

Edge headless の実 DOM で **62 件**。VBA ソースから JS を組み立て直す方式は D-3 と同じ。

★このハーネスが本物のバグを 2 件捕まえた★

1. `repairs:(prev&&prev.repairs?prev.repairs+1:0)` は **`repairs` が 0 のとき
   falsy で 0 に潰れる**。1 回目の自動修復が永久に記録されないところだった
2. `SpaProbeReset` が `window.__wv2p=null` にしていたので**世代が 1 に戻り**、
   いつ作り直したか追えなくなっていた。「版番号を壊して作り直させる」形に変更

★逆に、ハーネスが見逃したバグも 1 件あった★ (上記の事後報告2)。
`SpaProbeApplyJs` を空文字でスタブしていたため、**VBA 側の組み立てロジックを
一度も通していなかった**。設計原則98 (検証は本番と同じ経路で行う) を自分で破っていた。
直後にハーネスも本番経路へ直した。

### 確立した設計原則 (106〜109)

- **設計原則106 (D-4) 相手に持たせた設定は、空でも毎回送る。**
  「変わっていないから送らない」「空だから送らない」は、相手側に**前の状態が
  残り続ける**ことを意味する。送り手を唯一の権威にするなら、毎回上書きする。

- **設計原則107 (D-4) 外部の対象を掴む識別子は、推測で書かず観測して決める。
  観測する道具を先に作る。**
  実サイトの id は自動生成に変わる。当てずっぽうで直すと、次に壊れたときに
  何が変わったのか分からない。候補を並べて★どれが当たったかを残す★のが最低限。

- **設計原則108 (D-4) 汎用の妥協と正確な指定を両方用意し、既定は汎用側に倒す。**
  居座る要求の除外がこれ。既定 (時間による足切り) で知らないサイトでも動き、
  名指しの除外や明示シグナルで正確さと速さを取り戻せる。
  ★妥協で何を捨てているかを文書に書くこと★ が条件。

- **設計原則109 (D-4) 「落ち着いた」は観測できる範囲でしか判定できない。
  観測できない描画は待てないと明記する。**
  canvas / WebGL は DOM にも通信にも現れない。判定できないものを
  「判定できるふり」で返さないこと。

### 確立した仕様事実 (56〜61)

- **仕様事実56 (D-4) Google Maps の地図は canvas / WebGL で描かれ、タイル取得も
  アニメーションも MutationObserver にも fetch/XHR にも現れない。**
  何もしない 15 秒で DOM 変化 1 回・通信 1 回だった。逆に言えば
  ★「地図が描き終わったか」は DOM 側からは判定できない★

- **仕様事実57 (D-4) `MutationObserver` のレコード配送はマイクロタスクなので、
  同一の JS ターン内で読んでも反映されていない。**
  `EvalSync` はターンが切れるので実機では問題にならないが、
  1 つの式の中で「変えてから読む」検証を書くと**偽の NG** が出る。

- **仕様事実58 (D-4) Google Maps の検索は `/search?tbm=map` を開いたまま結果を
  流し続ける。** 20 秒以上 in-flight のままになる。SPA の「完了」を通信の完了で
  測ろうとすると必ず踏む形。

- **仕様事実59 (D-4) 実サイトの id は自動生成に変わりうる。**
  Maps の検索ボックスは `#searchboxinput` から `id=ucc-1` になっていた。
  `name` 属性・`aria-label`・構造の方が寿命が長い。

- **仕様事実60 (D-4) `msedge --headless` の標準出力は、Git Bash や Python の
  `subprocess` から直接起動すると空になる。**
  PowerShell の `Start-Process -RedirectStandardOutput` 経由なら取れる。
  症状が仕様事実55 (Edge 起動中のハンドオフ) と同じ「終了コード 0・無出力」なので
  紛らわしい。

- **仕様事実61 (D-4) Excel がブックを開いていると `Workbooks.Open` は
  読み取り専用で成功する。** 書き込みも事後照合もメモリ上の複製に対して成功するので、
  ★ディスクに届いていないのに「書き戻した」と報告される★。

### 変更ファイル

`Wv2Pane.cls` / `Wv2Element.cls` (自動待ちのフック 1 行) / `Wv2Tests.bas` /
`tools/import.ps1` (読み取り専用ガード)。新規モジュールは無い。

### D-5 以降への申し送り

- ★**canvas の描画完了は観測できない**★ (仕様事実56)。地図を画像として保存する
  用途に効いてくる。**印刷も画面キャプチャも未実装**なので、やるなら
  `CapturePreviewAsync` (完了ハンドラ 1 本 + IStream) が直球。
  ★描画完了は「少し間を空けて 2 回撮り、同一なら止まっている」で外から判定できる★
- `QuerySelectorAll` と要素の寿命管理は D-5 のまま (D-2 から持ち越し)。
- `WaitVisible` (可視まで待つ) も未実装。
- ★Shadow DOM と iframe に届いていない★ (D-3 の申し送りのまま)。
- **業務用途の次の一歩は「住所 → 座標」を 1 本の関数にすること。**
  部品は全部揃っている (`.Value =` / `.Click` / `ArmUrlSignal` / `WaitSettled` /
  URL からの座標抽出)。

---
## D-5 段階 ― 実用: 住所 → 座標 (Wv2Maps)

### 位置づけ

★D 軸で作った部品を、業務でそのまま使える形に畳んだ最初の例。★
D-4d で Google Maps の住所検索が実サイトで通ることを実証したので、その手順を
1 本の関数にした。**新しい機構は何も足していない** ― D-2〜D-4 の組み合わせだけ。

### 追加した新規モジュール `Wv2Maps.bas`

| メンバ | 意味 |
|---|---|
| `MapsOpen(browser)` | Maps のタブを開いて操作できる状態まで待つ。`Wv2Pane` を返す |
| `MapsGeocode(pane, 住所, outLat, outLng, outName)` | 住所を検索して座標と正規化名を取る |
| `MapsLastError` | 失敗の理由 |

```vba
Dim p As Wv2Pane, lat As Double, lng As Double, nm As String
Set p = Wv2Maps.MapsOpen(UserForm1.CurrentBrowser)
If Wv2Maps.MapsGeocode(p, "東京都千代田区丸の内1-9-1", lat, lng, nm) Then
    Debug.Print lat & "," & lng & "  " & nm
End If
```

★同じ Pane を使い回せる★ ので住所を次々に処理できる (バッチ)。
`outName` には Maps が正規化した住所 (`〒100-0005 東京都千代田区丸の内１丁目９−１`)
が入るので、**表記ゆれの正規化にも使える**のがおまけ。

### ★なぜ独立モジュールに隔離したか★

セレクタも URL の形も**Google の都合で予告なく変わる** (設計原則75)。製品コア
(`Wv2Pane` / `Wv2Element`) に混ぜると、壊れたときにどこを直せばよいか分からなくなる。
**変わるものは 1 箇所に閉じ込める。**

この判断は実際に利得を生んだ。Maps の検索がストリーミング要求を開きっぱなしに
する件 (仕様事実58) への対処 ― `AddIgnoreNetwork "/search?tbm=map"` ― も
**Maps 固有の知識**なので `MapsOpen` の中に置いた。結果、1 件あたりの所要が
★10.4 秒 → 2.0 秒★ になった (`StaleInflightMs` の 10 秒を毎回待たなくなったため)。

### ★実機で踏んだバグ: 座標が 1 件ぶん遅れる★

最初の実装は「検索前の URL と違う URL になったら完了」で待っていた。3 件続けて
引いたところ:

| | 名前 | 座標 |
|---|---|---|
| (1) 東京駅 | 正しい | 正しい |
| (2) 大阪城 | 正しい | ★東京駅のまま★ |
| (3) 札幌 | 正しい | ★大阪城のまま★ |

原因: Maps は検索を押すと**まず `/maps/search/<検索語>/@<前の中心>` に変わり**、
そのあとで `/maps/place/<名前>/@<新座標>/data=!3d<緯度>!4d<経度>` に落ち着く
(仕様事実62)。最初の変化で掴んでいたので、タイトルは新しいのに座標は前のまま、
という一件ずれになった。

★正しい待ち条件は「`/place/` と `!3d` の両方を含むこと」★
`!3d<緯度>!4d<経度>` は**場所が確定して初めて付く** (仕様事実63)。

★**1 件目だけは通っていた**★ 最初の検索は `/maps` からの遷移でタイミングが
噛み合うため。**3 件並べたテストでなければ見逃していた。**

### 座標をどこから取るか

- 第一候補: `!3d<緯度>!4d<経度>` … ★場所そのものの座標★
- 第二候補: `/@<緯度>,<経度>,<ズーム>` … 地図の中心
- ★`Val` を使う (`CDbl` ではない)★ `CDbl` はロケール依存で小数点が `,` の環境で
  壊れる。`Val` は常に `.` を小数点として読み、数字でない文字 (`!` や `,`) で止まる。

### 戻り値の規約 (設計原則93 を踏襲)

| | 意味 |
|---|---|
| `True` | `/place/` に着いた = 1 件に確定した |
| `False` + `ambiguous` | 候補が複数 (`/search/` に留まった)。★座標には地図の中心が入る★ |
| `False` + `no-searchbox` / `no-search-trigger` | 構造が変わった (`Test_D4_Dom` で調べ直す) |
| `False` + `timeout-url` / `no-coords` | 時間切れ / URL から座標を読めなかった |

### 実機検証の結果

| 住所 | 座標 | 所要 |
|---|---|---|
| 東京都千代田区丸の内1-9-1 | 35.6824246, 139.7682844 | 3.7 秒 |
| 大阪府大阪市中央区大阪城1-1 | 34.6852142, 135.5257536 | 2.0 秒 |
| 北海道札幌市中央区北1条西2丁目 | 43.0626377, 141.3539212 | 2.1 秒 |
| ZZZZ存在しない住所ZZZZ | ― | `False` + `ambiguous` |

**14 件すべて OK。** `Test_D5_Geocode` で再現できる。

### 確立した設計原則 (110)

- **設計原則110 (D-5) 状態を持つ相手は「2 回目」で試す。**
  1 件だけの検証は、**前の状態が残る類のバグを構造的に見逃す**。
  D-5 の座標ずれ (1 件目だけ正しい) も、D-4 の除外リストが空にできなかった件
  (設計原則106) も、2 回目で初めて出た。
  ★実サイトの検証は最低 3 件並べる★ ― 2 件だと「たまたま」と区別が付きにくい。

### 確立した仕様事実 (62〜63)

- **仕様事実62 (D-5) Google Maps の検索は、まず `/maps/search/<検索語>/@<前の中心>`
  に変わり、あとから `/maps/place/<名前>/@<新座標>/data=...` に落ち着く。**
  「URL が変わったら完了」では早すぎる。

- **仕様事実63 (D-5) Maps の URL の `!3d<緯度>!4d<経度>` は場所そのものの座標、
  `/@<緯度>,<経度>` は地図の中心。** 場所が確定して初めて `!3d` が付くので、
  完了判定の目印としても使える。

### 変更ファイル

`Wv2Maps.bas` (**新規**) / `Wv2Tests.bas`。製品コアは無変更。

### 申し送り

- **シート連携は D-5b で入れた** (下記)。
- **経路 (2 地点の所要時間・距離)** は未着手。住所検索より複雑
  (出発地と目的地の 2 つの入力欄、経路手段の選択がある)。
- ★`ambiguous` のとき候補一覧から選ぶ手段が無い★ 実務では「候補が複数なら
  1 番目を採る」が欲しくなる場面がある。`QuerySelectorAll` (D-6) が入れば書ける。

---
### D-5b: シート連携 (住所の列 → 座標の列)

```vba
Wv2Maps.MapsGeocodeSheet ActiveSheet
```

| 列 | 内容 |
|---|---|
| A | 住所 (入力) |
| B / C | 緯度 / 経度 |
| D | 正規化後の住所 |
| E | 状態 (`ok` / `ambiguous` / `not-found` / `no-searchbox` …) |

列の位置は引数で変えられる。1 行ごとに書き込んで `DoEvents` を回すので、
**処理中の画面で埋まっていく**。

★**途中で止めても続きから再開できる**★ 状態が `ok` の行は飛ばす。100 件の途中で
止めてももう一度呼べば残りだけ処理する。やり直したいときは状態列を消す。

### ★実機で踏んだバグ 2 件 ― どちらも「2 回目」で出た★

#### 1. でたらめな住所に、前の行の座標が付いた

```
4 行目: 34.6852142 / 135.5257536 / ZZZZ存在しない住所ZZZZ / ambiguous
```

検索が解決しなかったので**地図が動かず**、`/@` に前の行 (大阪城) の位置が残って
いただけ。当初の方針「候補が複数なら中心座標を書き残す」が裏目に出た。
状態欄を見れば分かるとはいえ、★`VLOOKUP` が拾えば間違ったデータとして混ざる★。

**直し方**: ★検索前後で座標が変わったかを見る★

| | 意味 | 座標 |
|---|---|---|
| 地図が動いた | 候補が複数 (広域が指定された等)。中心座標に意味がある | 書く (`ambiguous`) |
| 動いていない | 何も見つからなかった。★前の検索の残りかす★ | **書かない** (`not-found`) |

#### 2. 2 回目の実行で、また `ambiguous` に落ちた

`MapsGeocodeSheet` は毎回 `MapsOpen` で新しいタブを開く。2 回目は済んだ行を飛ばすので
**地図が一度も動かないまま**でたらめな住所を引くことになり、比較の基準になる座標が
無い (開いた直後の URL は `https://www.google.com/maps` で `/@` を含まない) ため
「動いたか判定できない」→ `ambiguous` に落ちた。

**直し方**: `MapsOpen` で ★URL に `/@` が入るまで待つ★ (1 秒ほどで付く) + 
それでも基準が無ければ**保守的に `not-found`** にする。
★怪しい座標を書き残すより「見つからなかった」と言う方が害が小さい★

### 確立した設計原則 (111)

- **設計原則111 (D-5b) 判定できない値は書き残さず、理由を残す。**
  「一応それらしい値を入れておく」は、後段 (人でも数式でも) が**それを信じる**。
  空欄 + 理由なら「無い」ことが伝わる。★状態欄があるから大丈夫、は成り立たない★
  ― 参照する側は値の列しか見ないことがある。

### 実機検証の結果 (D-5b)

**7 件すべて OK。** 3 行 (うち 1 行はでたらめ) を処理して 2 行が `ok`、
でたらめな行は**座標欄が空で `not-found`**。★1 回目と 2 回目で判定がぶれない★
ことも確認した (再開の経路)。1 件あたり 0.9〜1.8 秒。

### 変更ファイル (D-5b)

`Wv2Maps.bas` / `Wv2Tests.bas`。製品コアは無変更。

---
## D-6 段階 ― QuerySelectorAll と寿命管理

### 位置づけ

D-2 から持ち越していた「複数返し」と「レジストリの伸び」を片付ける。
D-5 で積んだ宿題 (Maps の候補一覧から選ぶ) の前提でもある。

### 論点と決定 (6 件を一括合意)

| # | 論点 | 決定 |
|---|---|---|
| 1 | スコープ | `QuerySelectorAll` + 寿命管理 (`WaitVisible` は別段階) |
| 2 | 複数の返し方 | ★**`Collection`**★ |
| 3 | 件数の上限 | 既定 200。★打ち切ったら知らせる★ |
| 4 | 寿命管理 | ★**見せるだけ。勝手に捨てない**★ |
| 5 | JS 側の返し方 | ハンドルをカンマ区切りの**文字列**で返す |
| 6 | 検証 | 自前 HTML + 実サイトは Maps の候補選択 |

### ★論点2: なぜ Collection か★

`For Each el In els` と書けて VBA に最も自然。0 件は `Count = 0` で素直。

- ★配列にしなかった理由★ **空配列の `UBound` は実行時エラー**になり、呼び出し側に
  毎回の防御を強いる。
- ★独自クラス (`Wv2Elements`) にしなかった理由★ `For Each` を効かせるには
  メンバー属性行 (`Attribute Item.VB_UserMemId = 0`) が要るが、
  ★`import.ps1` は属性行を落とす仕様★ (仕様事実47) なので**往復と相性が悪い**。
  ★言語機能の選択が、往復ツールの制約で決まった例★

### ★論点4: 寿命管理は「見せるだけ」★

`QuerySelectorAll` は 1 回で 200 個積みうるので、レジストリは D-2 の想定より速く伸びる。
だが**古いものを勝手に捨てると、手元の `Wv2Element` が黙って stale になる**。
D-2 論点3 (解放しない) と設計原則91 (権威は JS 側) がそのまま効く。

| 手段 | 内容 |
|---|---|
| 監視 | `ElementCount` で今の数を見せる |
| 警告 | 5000 を超えたら `LogW` で知らせる (捨てはしない) |
| 手動 | `ClearElementRegistry` (D-2 から在る) |

### 追加した Public メンバ

| メンバ | 意味 |
|---|---|
| `QuerySelectorAll(selector, maxCount)` | `Wv2Element` の `Collection` (既定 200 件) |
| `LastAllTruncated` | 直前の呼び出しが上限で打ち切られたか |
| `ElementCount` | レジストリに積まれている数 (失敗時 -1) |

### 実装

D-2 の `FindOne` を `push` からループに変えただけ。返す JSON は
`{g:世代, n:ページの実数, k:返した数, cut:打ち切ったか, h:'0,1,2', tot:積んだ総数}`。
★ハンドルは文字列★ (`Wv2Json` は配列を扱えないので、パーサを拡張するより
`Split` の方が小さい ― 論点5)。

### 実機検証の結果

**21 件すべて OK。** 事前の Edge headless は 73 件。

- 文書順が保たれる (`target,noise,slot`)
- 0 件は `Count=0` + `LastEvalOk=True` / 不正セレクタは `Count=0` + `LastEvalOk=False`
- 打ち切ると `LastAllTruncated=True` + 警告ログ (「ページには 3 件あるが 2 件で止めた」)
- `ElementCount` 11 → 掴むたびに増える → `ClearElementRegistry` で 0 →
  ★手元の要素が stale になる★

### 変更ファイル

`Wv2Pane.cls` / `Wv2Tests.bas`。

---
## 外部からの提言 — SPA における「完了待ち」の判定方式 (2026-08-20 受領)

★段階ではない。D-3 / D-4 を設計するときの入力として記録する。★

### 出典

**SeleniumVBA プロジェクトの参画者**から本プロジェクトに寄せられた提言。
たーぼーさん経由で受領。原文:

> SPA対応について少し気になったのですが、HistoryChanged や DOMContentLoaded などを
> 捕捉する仕組みはすでに実装されていますよね。
>
> 一方で、ボタンクリック後に fetch/XHR → React/Vue等によるDOM更新、というように
> 複数の非同期処理が連鎖するケースでは、これから作られる自動操作ラッパー側で
> 「次の操作へ進んでよい」と判断する完了待ちは、どのような方式を採用するのか
> 気になります。
>
> DOM条件待ち、MutationObserver、Network Idle、WebView2の各種イベントなど
> 色々なシグナルが使えそうですが、SPAの場合はどれか一つを見れば確実、
> という決定打がないので悩みどころですね。

### なぜ効くのか

**コンセプト①「Web ページを自動制御することに優れた使いやすさ」の核心を突いている。**

D-2 までで揃ったのは「**今の DOM を読む**」手段 (`EvalSync` / 要素レジストリ /
世代判定) であって、「**変化し終わるのを待つ**」手段はまだ何も無い。
D-3 (`.Click` / `.Value =`) を入れた瞬間に、**操作 → 待ち → 次の操作**という
連鎖が必ず必要になる。そこで待ち方を誤ると、
「ローカルでは通るのに本番で落ちる」自動化になる。

現状の持ち物と、それぞれの届く範囲:

| 持ち物 | 実装済み | SPA の連鎖に届くか |
|---|---|---|
| `NavigationCompleted` | 第9.4b | **届かない** (SPA の画面遷移では発火しない) |
| `DOMContentLoaded` | 第9.9 | **届かない** (初回ドキュメントのみ) |
| `HistoryChanged` | 第9.9 / 9.10 | **部分的** (pushState を伴う遷移は拾えるが、伴わない再描画は拾えない) |
| `EvalSync` | D-1 | 任意の JS を同期評価できるので、**待ちの土台にはなる** |
| 要素レジストリ + 世代 | D-2 | 「DOM が作り直されたか」は分かるが、「落ち着いたか」は分からない |

### 提言が挙げているシグナルと、現時点での見立て

**まだ決めない。** D-3 / D-4 の論点として一括合意にかけるための材料として置く。

| シグナル | 長所 | 弱点 |
|---|---|---|
| **DOM 条件待ち** (指定のセレクタが現れる/消える/テキストが変わるまで) | **意図が明示的**。誤検知が少ない。実装が軽い (`EvalSync` のループで足りる) | 呼び出し側が「何を待つか」を毎回書く必要がある。使いやすさの負担 |
| **MutationObserver** | DOM の変化そのものを捉える。汎用 | 「変化が止まった」の判定に**静穏時間 (quiet period)** の概念が要る。アニメーションや広告で永久に静まらないページがある |
| **Network Idle** | フレームワーク非依存。fetch/XHR の連鎖に強い | WebView2 から直接は取れない。`fetch` / `XMLHttpRequest` を JS 側でラップして数える必要がある (**設計原則92 の自己生成プロローグと相性が良い**)。ロングポーリングや WebSocket で永久に idle にならないページがある |
| **WebView2 の各種イベント** | 既に配線済み | 上表のとおり SPA の連鎖には届かない |

**現時点の私見 (未合意)**: 決定打が無いという指摘はそのとおりで、
**単一のシグナルを選ぶのではなく「合成 + 明示的な打ち切り」に寄せる**のが筋に見える。

- 既定は「**DOM 条件待ち**」— 使う側が待ちたいものを書ける形を第一級にする。
  コンセプト①は「万能な自動判定」ではなく「**書きやすさ**」なので、
  `WaitFor("#result", 5000)` のような形が本命になりうる。
- 補助として「**静穏待ち**」(MutationObserver + Network カウンタの合成) を用意し、
  待つ対象が特定できないときの保険にする。
- **必ずタイムアウトを持たせ、超えたら失敗として返す** (設計原則93 の
  `LastOk` / `LastError` の型に載せる)。★仕様事実20 / 14 があるので、
  待ちループは必ず `EvalSync` と同じ in-callback ガードの下に置くこと。★

### 着手するときの注意

- ★**待ちは D-3 と同時に要る**★ `.Click` を入れてから「待ちは次の段階で」とすると、
  その間の自動化は事実上使えない。D-3 の論点に**待ちの最小形を含めるか**を必ず問うこと。
- `fetch` / `XHR` のラップは**ページ側に痕跡を残す**ので、設計原則92 (注入しない /
  使う式が自分で用意する) との整合を先に決める必要がある。
  自己生成プロローグでカウンタを毎回作り直す形にできるかが鍵。
- SeleniumVBA には同じ問題に対する既存の解がある可能性が高い。
  **着手前に SeleniumVBA の待機 API を調べる価値がある** (提言者に聞けるなら聞く)。

### 提言者と、紹介された参考プロダクト (2026-08-22 追記)

**提言者は sele_chan さん** (Excel-Fun コミュニティ所属)。

★**同氏は下記プロダクトの作者本人**★ (2026-08-22 に判明。当初は「紹介されたプロダクト」と
誤って記録していた)。ハンドルを場によって使い分けている:

| 場 | ハンドル |
|---|---|
| GitHub / Medium | `hanamichi77777` |
| note / Qiita / Discord | `sele_chan` |

根拠: Discord で「v4.3 にアップデートして…追加しました」「Shadow 専用メソッドを二重に
増やす設計にしたくないので」「実装完了しました」と**設計を決める立場**で書いている。
Qiita 記事も「公開版は以下の Github リンクとなります」と自身のリリースを案内する形。

sele_chan さんは記事も公開している。SeleniumVBA まわりで手が止まったら当たる先:

- `https://note.com/sele_chan`
- `https://qiita.com/sele_chan`

**WebDriver BiDi for SeleniumVBA** (作者 sele_chan さん = GitHub の hanamichi77777、MIT、v4.3)
`https://github.com/hanamichi77777/WebDriver-BiDi-for-SeleniumVBA`

★**ここに書いたのは README を読んだだけの二次情報**★ (`src/` は `BiDiCommandWrapper.cls`
467 KB / `BiDiSocketCommunicator.cls` 203 KB / `BiDi_Sample.bas` 26 KB の 3 本。中身は未読)。
実際に採り入れるときは原典を当たること。

#### 何をするものか

SeleniumVBA の**拡張** (置き換えではない)。VBA から **W3C WebDriver BiDi** を
**ネイティブ WinSock の WebSocket** で直接話し、ブラウザ側のイベントを購読する。
狙いは本プロジェクトと**完全に同じ**:

> 「リクエスト完了 = ページ準備完了」が成り立たない SPA で、次の操作へ進んでよいと
> 判断する材料をどう得るか

検証ベンチマークが **ServiceNow のログインフォーム** (Shadow DOM + 非同期 UI) と
**Google Flights の航空券検索** (動的サジェスト / 運賃カレンダー / 非同期結果) という、
最も難しい部類の実サイトなのが目を引く。

#### 待ちの設計 (★ここが本題★)

| 仕組み | 内容 |
|---|---|
| **idle consensus** | ネットワーク活動 + Fetch/XHR カウンタ + DOM ミューテーション + **静穏窓 (slackMs)** の**合成**で「十分安定した」を推測する |
| **`ArmNetworkSignal(pattern)`** | 次の 1 回だけ、特定のネットワーク要求/応答を待つ |
| **`ArmContentSignal(xpath)`** | **既存の** DOM 部分木が書き換わるのを待つ。★arm 時に対象が無ければ即エラー★ |
| **`ArmVisibilitySignal(xpath)`** | 要素が可視になるのを待つ |
| **`AddIdleIgnoreNetworkPattern`** | 要求は通すが**静穏判定から除外**する (常時鳴っているテレメトリ用) |
| **`ExecuteEnableResourceBlocking`** | 要求そのものを**送らせない** (より強い。ページの挙動を変えうる) |
| **`waitForCompletion`** | 操作ごとに待つ/待たないを選ぶ。安定窓 (例 1000ms) も操作ごとに指定 |
| **Discovery Log** | ネットワーク / DOM ミューテーション / 抑制したノイズ / slackMs / 安定判定を**同一タイムラインに記録**する診断ツール |

★**最重要の型は「arm してから act する」**★ シグナルは**ワンショット**で、
次の待ちに消費される。同じ操作を 2 回するなら 2 回 arm し直す。

#### 作者自身が明記している限界 (誠実で、そのまま我々にも当てはまる)

- ★**SPA の完了は「推測」であって保証ではない**★ 内部の論理的完了は証明できないし、
  将来の遅延処理を排除もできない
- **重要な操作には明示シグナルを使う**。静穏だけに頼らず `ArmContentSignal` /
  `ArmNetworkSignal` を操作の直前に arm する方が安全
- **ignore パターンを広く取りすぎると、意味のある要求まで除外して早すぎる STABLE を出す**

#### 我々との差分 ― 借りられるもの / 借りられないもの

| | 向こう | こちら (WebView2 + `EvalSync`) |
|---|---|---|
| 経路 | WebDriver BiDi (WebSocket) | `ExecuteScript` の往復のみ |
| ネットワーク観測 | **ページに触らず**プロトコルから取れる | ★`fetch` / `XHR` をページ内でラップするしかない★ |
| DOM 変化 | BiDi + ページ内プローブ | MutationObserver をページ内に置く |
| 依存 | WebDriver / ブラウザの BiDi 端点が要る | 追加の依存はゼロ |

**通信経路そのものは借りられない。** BiDi 端点が要るので、外部ライブラリゼロという本プロジェクトの
前提と噛み合わない。**借りるべきは API の形と限界の扱い方。**

#### D-4 への具体的な示唆

1. ★**「arm してから act」が設計原則92 の緊張を解く**★
   ネットワーク数え上げは「要求が始まる前にラップを仕込む」必要があり、自己生成プロローグ
   (遅延生成) とは本来相性が悪い。**しかし arm-then-act なら、arm した時点でラップを作れば
   必ず操作より前になる。** 「arm 時点以降のみを数える」と仕様を切れば、
   `AddScriptToExecuteOnDocumentCreated` を持ち出さずに済む可能性が高い。
2. **静穏待ちは単独の API にせず、合成 + 打ち切りにする。**
   我々の `LastEvalOk` / `LastError` の型に載せ、★「推測である」ことを戻り値の意味に
   書き込む★ (例: `STABLE` / `TIMEOUT` / `UNKNOWN` を区別する)。
3. **ノイズ除外 (idle 判定から外す) と遮断 (送らせない) は別物として設計する。**
   我々に遮断の手段は今のところ無いので、まずは除外だけ。
4. **待ちのポリシーは操作ごとに選べるようにする。** D-3 の `.Click` は待たない設計なので、
   D-4 で「この操作の後は静穏まで待つ」を足す形になる。既定を変えるかは論点。
5. ★**Discovery Log 相当は我々にも作れるし、効果が大きい**★
   K-1 の `Wv2Log` が既にある。ページ内プローブが集めた「要求 / DOM 変化 / 判定」を
   タイムラインで吐けば、**「何を待つべきか」を推測でなく観測で決められる**。
   向こうがログを AI に食わせて分析させる運用まで想定しているのも、
   本プロジェクトの開発サイクル (Claude がログを直接読む) とそのまま噛み合う。

#### ついでに見えた我々の穴 (D-4 以降の宿題)

- ★**Shadow DOM を貫通できない**★ `document.querySelector` は shadow root の中を見ない。
  ServiceNow のような実サイトでは必須 (向こうには `ExecuteShadowClick` がある)。
- ★**iframe を跨げない**★ 向こうは browsing context ID を指定して frame 内を直接叩く。
  こちらは `document` 直下しか見ていない。
- ファイル選択ダイアログ / ダウンロード / 新規タブ捕捉も向こうは扱っているが、
  こちらはコンセプト①②に照らして**優先度は低い** (新規タブは第9.27 で別途対応済み)。

### 参考プロダクトのその後 (2026-08-22 に共有を受けた)

作者の sele_chan さんから、`WebDriver BiDi for SeleniumVBA` v4.3 の状況を共有された。
★我々に直接効くものが 3 点ある。★

#### 1. ★手動操作中の DOM 変更記録★ (Discovery Log の拡張)

作者の説明する運用:

```
未知 SPA を手動で観測する
        ↓
Network + DOM を同一時間軸で見る
        ↓
何がデータ源なのか調べる
        ↓
何を完了条件にするか決める
        ↓
最終的な自動化方式を設計する
```

「最初に手掛けるサイトに対して手動操作によるログ記録を AI に投げると、
実装の前にどのような待機戦略で進めていくか指針が立てられる」

★**これは我々の道具立てでそのまま踏める**★ D-4e のプローブは自動操作でなくても
動くので、`SpaProbeLogging = True` にしてから**人が手で操作**し、
`SpaProbeDrainLog` で吸い出せば同じ記録が得られる。
**「実装の前に観測する」という順序そのものが設計原則107 と同じ思想。**

#### 2. VirusTotal 0 検知 (Bkav も含む)

v4.3 で 64 ベンダー中 0 検知。★誤検知王の Bkav もクリーン判定★。
我々の第9.30 からの宿題 (Bkav の誤検知) にとって参考になる。
VBA + WinSock の .xlsm でもゼロ判定は取れるということ。

#### 3. ★Shadow DOM は専用メソッドを増やさず既存メソッドを対応させた★

作者が踏んだ経緯と結論:

> ShadowDOM 内メソッドがクリックしか対応できていません。ただし、
> `ExecuteShadowInput` / `ExecuteShadowSelect` / `ExecuteShadowGetText` … と
> Shadow 専用メソッドを通常 DOM 用と二重に増やす設計にしたくないので
> 共通化が図れるよう実験していきたい

> 実装完了しました。通常 DOM で XPath を使うメソッドをすべて ShadowDOM でも
> 使えるようになりました。

★**我々が Shadow DOM に着手するときの出発点として借りられる**★
`Wv2Element` の側に Shadow 専用の双子 (`ShadowClick` 等) を作らず、
**掴む側 (`QuerySelector`) を shadow root 貫通に対応させる**のが筋、ということ。

なお「Shadow DOM に XPath が使えない」は我々にも同型の問題で、
`document.querySelector` は shadow root を貫通しない。貫通するには
`shadowRoot` を辿る再帰探索を JS 側に書くことになる (D-2 のプロローグに載る形)。

