# VBA-WebView2 開発メモ 第4部 — E 軸 (開発環境・ツール)

製品コードではなく**開発の足場**を扱う軸。リポジトリ構成、ブックとソースの往復、
検査ツール、Claude への指示の集約などがここに入る。D 軸と並行する **現役の追記先。**

E 軸の作業は原則として **`Wv2*` の製品コードを 1 行も変えない。** 変える必要が出たら
それは D 軸か K 軸の段階として切り出す。

---

## E-1 段階 — 開発の場を Claude Code へ移行し、ブックとの往復基盤を作る

### 位置づけ

2026-08-19、D-2 完了直後。**製品コードの変更はゼロ。**

これまでは通常のチャットで開発し、Claude が出力したモジュールをたーぼーさんが
ダウンロードして VBE にインポートし、開発メモも手作業で差し替えていた。
段階が進むにつれてこの受け渡しの手間が支配的になってきたため、ローカルのファイルを
直接読み書きできる Claude Code へ場を移す。

### 移行前の状態 (調査で判明したこと)

- **GitHub リポジトリは第9.10c 時点で止まっていた。** 現行モジュール
  (`Wv2Pane` / `Wv2Element` / `Wv2Json` / `Wv2Tests` / `Wv2TabBar` ほか) は
  **1 本も入っていなかった。** 入っていたのは 7/4 時点の `Wv2Browser.cls` と
  `Wv2Thunks.bas` の 2 本だけで、しかも未コミット。
- ルートの `開発メモ.md` は旧・単一ファイル版。4 分冊はリポジトリに無かった。
- **現行の実体は `Downloads` に散乱していた。** `Wv2Tests_22.bas` のように
  連番の付いたダウンロード残骸が数十本あり、どれが現行ブックの中身と一致するかは
  外からは保証できない状態。
- 現行ブック `WebView2_v0.5.2.xlsm` と開発メモ 4 分冊も `Downloads` にあった。
  4 分冊の行数 (509 / 6512 / 6629 / 365) が引き継ぎプロンプトの記載と一致したので、
  これが最新であることを確定できた。

### 論点と決定 (6 件を一括合意)

| # | 論点 | 決定 |
|---|---|---|
| 1 | **真実をどちらに置くか** | **`src/` の `.cls` / `.bas` を正**とする。ブックへは import で反映する。Code の編集・grep・`git diff` が効くのは この側だけ |
| 2 | ブックとの往復の手段 | PowerShell + Excel COM (VBIDE) でスクリプト化。`AccessVBOM=1` が**既に立っていた**ので追加設定は不要だった |
| 3 | フォルダ構成 | `src/` `forms/` `docs/` `book/` `tools/` に役割分割。ルート直下に散っていた `.xlsm` 11 本は `Old/` へ退避 |
| 4 | 旧・開発メモの扱い | `Old/` へ退避 (git 履歴に残るので情報は失われない) |
| 5 | `CLAUDE.md` の新設 | 引き継ぎプロンプトの役割を移す。**毎セッション自動で読み込まれるので、手で貼る作業が消える** |
| 6 | ブックを git 管理するか | `src/` を正にするので**節目だけコミット**。毎回入れると 900 KB のバイナリが履歴に積み上がる |

**フォームとドキュメントモジュールは自動化の対象外**とすることも併せて合意した。
`UserForm1` は `.frm` と `.frx` の対で `.frx` がバイナリ、`ThisWorkbook` / `Sheet1` は
そもそも Import で置き換えられないため。

### 確定したリポジトリ構成

| 場所 | 中身 | 性質 |
|---|---|---|
| `src/` | 標準モジュール 6 本 + クラスモジュール 9 本 | **★正★ ここを編集する** |
| `src/document/` | `ThisWorkbook` / `Sheet1` | 参照用 (Import では置換できない) |
| `forms/` | `UserForm1` / `TimerForm` | 手動運用。自動 import の対象外 |
| `book/` | `WebView2_v0.5.2.xlsm` | 現行ブック。節目だけコミット |
| `docs/` | 開発メモ 4 分冊 + `通常サンク方式.md` + `引き継ぎ_D-2完了時点.md` | |
| `tools/` | `export.ps1` / `check_cp932.py` | |
| `Old/` | 過去のブック・モジュール・旧開発メモ | **触らない** |
| `CLAUDE.md` | Claude への常設指示 | ルート直下 |

### ブックとの往復 (`tools/export.ps1`)

```
powershell -ExecutionPolicy Bypass -File tools/export.ps1 -Force
```

- ブックは **ReadOnly** で開き、**`AutomationSecurity = 3`** (マクロ強制無効) を設定してから
  開く。`Workbook_Open` を発火させないためで、これが無いと **エクスポートするだけで
  WebView2 が起動してしまう。**
- 閉じるときは `SaveChanges:=$false`。ブックは一切変更しない。
- 種別で振り分ける: 標準/クラス → `src/`、ドキュメント → `src/document/`、
  フォーム → `forms/` (**`-Forms` を付けたときだけ**)。
- 書き出した直後に **CP932 → UTF-8 変換**し、**CP932 へ戻して元バイト列と一致するか
  検証**する。一致しなければ変換を見送って原本のまま残し、警告を出す。
- `.frx` はバイナリなので一切触らない。

**逆方向 (`src/` → ブック) の `import.ps1` は未作成。E-2 で作る。**
現状は VBE で手動インポートするしかないので、**往復はまだ閉じていない。**

### ★発見: VBA のソースはプロジェクトのコードページで保持される★

エクスポート結果を配布物と全数照合したところ、**`⚙` `⟳` `⇄` `↔` `〜` `—` が
すべて `?` に化けていた。**

**切り分け**: エクスポートが落としたのか、ブックが既に失っているのかを判定するため、
`Export` ではなく **`CodeModule.Lines` を COM 経由 (BSTR = UTF-16) で読み出して**
UTF-8 で保存し、比較した。結果、**COM 経由で読んでも `?` のまま**だった。
すなわち**ブック側で既に失われている**。エクスポートの問題ではない。

一方 `★` は 1,517 個、`←` `→` `①②④` も無傷で残っていた。これらは CP932 にある文字。

**実測 (`src/` の `.bas` + `.cls` 全 17 本)**:

| 項目 | 数 |
|---|---|
| `?` の総数 | 238 個 |
| ★うち化けて `?` になったもの★ | **76 個** |
| 正当な `?` (URL のクエリ文字列 / JS の三項演算子 / イミディエイトの `?` 短縮形 / 疑問文) | 162 個 |

化けた 76 個の内訳:

| 文字 | コード | 個数 | 代替候補 |
|---|---|---|---|
| `〜` | U+301C WAVE DASH | 33 | `～` U+FF5E |
| `⚙` | U+2699 | 28 | `[設定]` |
| `⇄` | U+21C4 | 6 | `⇔` |
| `↔` | U+2194 | 5 | `⇔` |
| `⟳` | U+27F3 | 3 | `[更新]` |
| `—` | U+2014 EM DASH | 1 | `―` U+2015 |

**被害の範囲**: 全部**コメントと `Debug.Print` の案内文**の中にある。
**実行ロジックと HTML/JS は無傷。** タブバーの歯車ボタンは HTML の実体参照
`&#9881;` で描いているため、**画面の見た目には一切影響していない。**

配布物の該当行はすべて `?` 置換後の形で `src/` に存在することを確認済み
(照合できなかった行はゼロ)。**したがって上の 76 個が被害の全数。**

### 検査器 `tools/check_cp932.py`

```
python tools/check_cp932.py
```

`src/` を走査し、**CP932 で往復できない文字**を行・桁つきで報告して代替候補を出す。
問題があれば終了コード 1。

**★「encode できるか」では不十分★** 失敗モードが 2 つあるため:

| モード | 例 | 結果 |
|---|---|---|
| encode 自体が不可 | `⚙` `⟳` `⇄` `↔` `—` 絵文字全般 | `?` になる (目視で気づける) |
| **encode は通るが別の字に化ける** | `〜`→`～`、`−`(U+2212)→`－`、`¢`→`￠` | **黙って置き換わる** |

さらに **Python の `cp932` コーデックは Windows より寛容**で、`〜` (U+301C) は
Python では encode が通るが **VBA では `?` になる。** 往復判定ならどちらのモードも捕まる。

**検査器自身の検証**: `src/` に対しては問題ゼロ (ブック由来なので当然)。
そこで**既知の壊れる文字を含む配布物** (`Downloads/Wv2TabBar.cls`) に掛け、
歯車 4 個を正しい行・桁で検出し終了コード 1 を返すことを確認した。
★検査器は「発火すること」を確かめて初めて信用できる。★

### 実装上の事後報告 (設計原則79)

1. **`export.ps1` の既定でユーザーフォームを書き出さないようにした** (`-Forms` を付けたときだけ)。
   `.frx` は中身を変えていなくても**エクスポートのたびに 5 バイト単位で変化する**
   (OLE のタイムスタンプらしきもの) ことが分かり、毎回走らせると `git status` が
   汚れ続けるため。フォームは手動運用という合意とも整合する。
2. **引き継ぎプロンプトを `docs/` に保存する際、エスケープ列 5 箇所を散文に置き換えた。**
   保存文書内に `\"` や `<` がリテラルで残ると今後 grep で誤検出を招くため
   (引き継ぎ自身が警告している問題)。意味は変えていない。
3. **`docs/引き継ぎ_D-2完了時点.md` の冒頭に「歴史的記録であり現行ルールではない」注記を追加した。**
   Claude Code には `present_files` も `/home/claude/out/` も無いので、そのまま読むと誤誘導になる。

### 実施した検査と結果

| 検査 | 結果 |
|---|---|
| `src/` 全 17 本の文字コード | UTF-8 / BOM なし / CRLF、裸 LF ゼロ |
| CP932 への往復 | **全 17 本でバイト単位一致** (`export.ps1` が自動検証) |
| 配布物との全数照合 | 版ズレなし。差分は下記 3 種のみ |
| CP932 往復検査 | 問題ゼロ。検査器は既知の壊れる文字に対して正しく発火 |

配布物との差分は次の 3 種だけで、いずれも**意図された差**であることを確認した:

1. エクスポートが付けるヘッダー行 (`VERSION 1.0 CLASS` / `Attribute VB_Name` ほか) と末尾空行 2 つ
   — 引き継ぎの行数記載が一律 +2 ずれて見えた原因はこれ。**版ズレではなかった**
2. **VBA による識別子の大小統一** (`Value`→`value` / `Y`→`y` / `ptr`→`Ptr`)
3. VBE が付ける隠し属性 (`Attribute m_browser.VB_VarHelpID = -1`)

### 踏んだ事故

| 事故 | 原因 | 最速の診断法 |
|---|---|---|
| `.ps1` が構文エラーの山になった | BOM なしで保存したため Windows PowerShell 5.1 が CP932 として読み、日本語コメントが化けた | エラーが**日本語コメント行に集中**していたら文字コードを疑う |
| `export.ps1` が「Byte[] を SwitchParameter に変換できない」で落ちた | ローカル変数 `$raw` (バイト配列) が switch パラメータ `$Raw` と衝突。**PowerShell も識別子の大小を区別しない** | VBA の `eNum` / `Enum` (設計原則90) と同型。**新規識別子はパラメータ名とも小文字化して照合する** |
| `git push` が拒否された | リモートに未取得のコミット `a3de35b` (第9.29段階まで更新) があった | **force push は絶対にしない。** fetch して差分を読み、内容を照合してからマージする |
| 文字単位の差分検出がタイムアウト | 170 KB の文字列に `difflib.SequenceMatcher` を文字単位で掛けた | 大きいファイルは**行単位**で突き合わせる |

### 索引の記載の訂正

実ファイルで測定した結果、索引の「現行の状態」に誤りが見つかったので訂正した。

- **モジュール数 18 本 → 19。** D-2 で `Wv2Element` が増えた分が反映されていなかった。
  事故メモの診断法「モジュール数を数える (v0_5_2 は 18 本)」も **19** に訂正。
  内訳: 標準 6 + クラス 9 + フォーム 2 + ドキュメント 2 = 19。
- **ブックサイズ 683 KB → 888 KB** (909,245 バイト)。

### 変更ファイル

| ファイル | 内容 |
|---|---|
| `CLAUDE.md` | 新規。引き継ぎプロンプトの役割を移した常設指示 |
| `tools/export.ps1` | 新規 |
| `tools/check_cp932.py` | 新規 |
| `.gitignore` | 新規 (Excel のロックファイル / バックアップ / OneDrive の競合コピー) |
| `.gitattributes` | `*.xlsm` `*.xlsx` `*.frx` に `binary` を追記 |
| `src/` `forms/` `docs/` `book/` | 新設・配置 |

**`Wv2*` の製品コードは 1 行も変えていない。**

### 確立した設計原則 (94〜95)

## ★ 設計原則 94 — 変換を挟む往復は「戻したものが元と一致するか」で判定する ★

片道の成否 (encode できたか) では不十分。**best-fit 変換のように「成功したのに
別のものになる」経路がある。** CP932 では `〜`(U+301C) が `～`(U+FF5E) に、
`−`(U+2212) が `－`(U+FF0D) に黙って置き換わる。encode は成功するので
エラーにならず、目視でも気づけない。

往復させて元と一致するかを見れば、encode 不可と best-fit 置換の**両方**が同じ
判定で捕まる。`export.ps1` の変換検証も `check_cp932.py` もこの形にしてある。

※ 実装側の教訓として、**検証に使うライブラリが本番と同じ変換表を使っているとは限らない。**
   Python の `cp932` は Windows の CP932 より寛容で、`〜` を通してしまう。
   往復判定ならライブラリ差にも耐える。

## ★ 設計原則 95 — 開発環境の制約は、その場で直さず検査器にして常設する ★

設計原則90 (予約語チェックの常設化) の一般化。一度踏んだ環境固有の制約は、
**次に踏むのが数か月後**であることが多く、そのときには経緯を忘れている。

直した瞬間に検査器へ落とし、定型の検査セットに組み込む。**そして検査器は
「既知の悪いデータに対して発火すること」を確かめてから信用する。**
`src/` に問題が無いのは当たり前 (ブック由来なので既に失われている) で、
それだけでは検査器が動いている証拠にならない。

### 確立した仕様事実 (35〜38)

## ★ 仕様事実 35 — VBA のソースはプロジェクトのコードページで保持され、表現できない文字は import 時に永久に失われる ★

日本語環境ではコードページ 932。CP932 で表現できない文字は、モジュールを import した
時点で `?` に置き換わり、二度と戻らない。**ファイル側の文字コードとは無関係**で、
UTF-8 で渡そうが同じ結果になる。失敗モードは 2 つ (設計原則94 参照)。

現行ブックには既にこの被害が 76 文字分入っている
(`〜`×33 / `⚙`×28 / `⇄`×6 / `↔`×5 / `⟳`×3 / `—`×1)。
**すべてコメントと `Debug.Print` の案内文の中で、実行ロジックと HTML/JS は無傷。**

## ★ 仕様事実 36 — VBE の Export は CP932 / CRLF / BOM なしで書き出す ★

`VBComponent.Export` が書くファイルはシステムの ANSI コードページ (CP932) で、
改行は CRLF、BOM は付かない。`.frx` はバイナリ。

逆に **Import は BOM なし UTF-8 を受け付ける** — これは第9.x 以降 Claude が出力した
UTF-8 ファイルを取り込み続けて日本語コメントが正しく入っている実績からの推定で、
**E-2 (import.ps1) で直接確認する。**

## ★ 仕様事実 37 — `.frx` はエクスポートのたびに数バイト変化する ★

フォームの中身を一切変えていなくても、`.frx` の内容は毎回変わる
(`TimerForm.frx` で 5 バイト、`UserForm1.frx` で 28 バイト。
5 バイト単位のまとまりで変化しており、OLE のタイムスタンプらしい)。

このためフォームを毎回エクスポートすると差分が汚れる。`export.ps1` の既定では
書き出さない。

## ★ 仕様事実 38 — VBA は識別子の大小を最初の宣言に合わせて統一し、既存の出現箇所も書き換える ★

`Wv2Element` の `Public Property Get Value()` はブック内で `value` になっており、
`Wv2Thunks` の `ByVal Y As Long` は `y`、`ByVal ptr As LongPtr` は `Ptr` になっていた。

**VBA が同名識別子の綴りをプロジェクト全体で統一するため。** 動作は変わらないが、
エクスポートするとソースの見た目が変わるので、**配布物と突き合わせるときに
「差分が出た = 版ズレ」と誤読しないこと。**

### E-2 以降への申し送り

- **E-2: `import.ps1` (最優先)。** これが無いと `src/` を編集してもブックへ届かない。
  K-1 も D-3 も VBA を書き換えるので、**先に塞ぐ必要がある。**
  合意済みの設計:
  1. import 前にブックを `book/backup/` へタイムスタンプ付きで複製する (`.gitignore` 済み)
  2. 対象は `src/*.bas` と `src/*.cls` のみ。`src/document/` と `forms/` は対象外
  3. ★`VBComponents.Remove` + `Import` は使わない★ 削除が遅延して同名 Import が
     `Wv2Pane1` に化ける既知の罠がある。**`CodeModule` を全削除して `AddFromString` で
     流し込む** (コンポーネントを消さないので名前が保たれる)
  4. 内容が変わったモジュールだけ入れる
  5. 事前ゲート: `check_cp932.py` を必ず走らせ、1 件でも出たら**中止**
  6. 事後検証: import 後に自動で export し直し、`src/` と一致することを確認して往復を閉じる
  7. Excel は不可視で開いて保存する
- **化けた 76 文字の修復**は別段階。復元しても import すればまた `?` に戻るので、
  **CP932 にある代替字への置換**が必要 (上の内訳表に候補を記載済み)。
  `Debug.Print` の案内文が「? を押して設定タブを開く」では意味が通らないので、
  K-1 (ログ基盤) の前後で片付けたい。
- **`src/document/` と `forms/` の更新手順**は未整備。フォームを触った回は
  `-Forms` を付けて手で回す運用になる。

---

## E-2 段階 — `import.ps1` でブックとの往復を閉じる

### 位置づけ

E-1 でブック → `src/` は自動化したが逆方向が無く、**往復が閉じていなかった。**
`src/` を編集してもブックへ届かないので、K-1 も D-3 も着手できない。これを塞ぐ回。

**製品コードの変更はゼロ。** 検証はすべてブックの複製に対して行い、
`book/WebView2_v0.5.2.xlsm` は最初から最後まで md5 が変わっていない。

### 実装した設計 (E-1 で合意した 7 点をそのまま)

1. import 前にブックを `book/backup/` へタイムスタンプ付きで複製する
2. 対象は `src/*.bas` と `src/*.cls` のみ。`src/document/` と `forms/` は対象外
3. **`VBComponents.Remove` + `Import` は使わない。** `CodeModule` を全削除して
   `AddFromString` で流し込む (コンポーネントを消さないので名前と属性が保たれる)
4. 内容が変わったモジュールだけ入れる。1 本も変わらなければブックを保存しない
5. 事前ゲート: `check_cp932.py` を走らせ、1 件でも出たら**中止**
6. 事後検証: 書き戻した直後に読み直して照合する
7. Excel は不可視で開いて保存する

### ★踏んだ壁 1: `CodeModule.Lines` とメンバー属性行の非対称★

最初の下見で、15 本中 12 本は完全一致したのに **3 本だけ行数が増えて「要更新」になった。**

| モジュール | ブック | `src/` | 差 |
|---|---|---|---|
| `Wv2Browser` | 2583 | 2587 | +4 |
| `Wv2NavBar` | 1045 | 1046 | +1 |
| `Wv2TabBar` | 1263 | 1264 | +1 |

原因は **`Attribute m_browser.VB_VarHelpID = -1` のようなメンバー属性行**。
`src/` の該当ファイルを数えると 4 / 1 / 1 件で、行数差と**完全に一致**した。
いずれも `WithEvents` メンバの宣言直後に付く。

実測で分かったこと:

- **`CodeModule.Lines` はメンバー属性行を一切返さない** (VBE が隠している)
- **`Export` は書き出す**
- **`AddFromString` は受け付け、メンバー属性として正しく保存する**
  (書き戻した後に `Export` し直すと同じ位置に復元されていた)

したがって **書き込みには残し、比較のときだけ両側から落とす** のが正しい。
落として書き込むと属性そのものが消えてしまい、逆に落とさずに比較すると
この 3 本が永久に「要更新」のままになる。

### ★踏んだ壁 2: `AddFromString` は末尾に改行を 1 つ足す★

最初の書き込み試験で、`Wv2NavBar` が 1045 → 1047 行になった。
保存して開き直すと 1046 行で、`Export` し直すと `src/` との差は**末尾の空行 1 行だけ**。

つまり `AddFromString` は渡した文字列の末尾に改行を 1 つ足す。
**末尾の空行を落とさずに渡すと、import のたびに 1 行ずつ増え続ける。**
`Get-CodeBody` で末尾の空行を全部落とすようにして解決した。

※ import 直後の `CountOfLines` (1047) と、保存して開き直した後の値 (1046) が違った。
   **書き込み直後の `CountOfLines` は信用しないこと。**

### 実機検証の結果 (全合格)

**すべてブックの複製に対して実施した。** 本物のブックは検証の前後で md5 不変
(`FCF5E0D94B6D242371DFDA00EBF45C56`)。

| # | 検証 | 結果 |
|---|---|---|
| 1 | 無傷の複製に対する下見 | **15 本すべて「変更なし」**。ヘッダー除去・メンバー属性・末尾空行の扱いが正しく、同時に `src/` がブックに忠実であることも確認できた |
| 2 | 書き込み経路 (`Wv2Url` の 2 箇所にマーカーを入れて `-Apply`) | **`Wv2Url` だけが 153 → 155 で書き戻され、他 14 本は無変更。** 差分 import が効いている |
| 3 | 書き戻し後に `Export` し直してバイト照合 | **14 本がバイト完全一致。** `Wv2Url` の差は Export が付ける末尾空行 1 行のみ。マーカーは 2 行目と 155 行目の**意図した位置**に入っていた |
| 4 | ★冪等性★ 同じ `src` でもう一度下見 | **0 / 15。** 累積しない |
| 5 | ★冪等性★ export し直した結果で下見 | **0 / 15。** Export が付ける末尾空行を import 側が吸収している |
| 6 | 本物のブックに対する下見 | **0 / 15。** `src/` とブックは同期している |

検証 4 と 5 が**この回の受け入れ条件**だった。1 回目が成功しても 2 回目で差分が出るなら
往復は閉じていない。

### 実装上の事後報告 (設計原則79)

1. **既定を「下見 (dry run)」にした。** 合意した 7 点には無かった追加。
   ブックを書き換えるツールなので、`-Apply` を明示しない限り読み取り専用で開いて
   「何が変わるか」を報告するだけにした。
2. **`-SkipGate` と `-NoBackup` を付けた。** 検証中に事前ゲートやバックアップを
   飛ばしたい場面があったため。通常運用では使わない。
3. **新規モジュールの追加は実装しなかった。** `AddFromString` は既存コンポーネントの
   中身を差し替えるだけなので、`src/` にあってブックに無いモジュールは
   「ブックに存在しない」と報告して飛ばす。VBE で空のモジュールを作ってから
   再実行する運用にした。`VBComponents.Add` で作る案もあったが、
   **壁 1 で見たとおりコンポーネント生成まわりは挙動が読みにくい**ので、
   実需が出るまで踏み込まない判断。

### 文字コードについて分かったこと

`AddFromString` は BSTR (UTF-16) を受け取るので、**ファイル側の文字コードの問題は
PowerShell が UTF-8 として読んだ時点で解決している。** `Import` は使わないため、
仕様事実36 の「Import が BOM なし UTF-8 を受けるか」は**この往復では問われない。**
(VBE で手動インポートする場合には依然として関係する。)

ただし VBA が受け取った文字列を CP932 で保持することは変わらないので、
**仕様事実35 の制約はそのまま効く。** 事前ゲートは必須。
検証 3 で日本語コメントを大量に含む 14 本がバイト完全一致したことで、
`UTF-8 ファイル → PowerShell → AddFromString → ブック → Export → UTF-8 ファイル`
の全経路が無損失であることを確認できた。

### 変更ファイル

| ファイル | 内容 |
|---|---|
| `tools/import.ps1` | 新規 |
| `CLAUDE.md` | 往復の手順に import を追記。索引の行数・原則/事実の上限番号を更新 |

### 確立した設計原則 (96〜97)

## ★ 設計原則 96 — 破壊的なツールは既定を「下見」にし、実行に明示のフラグを要求する ★

`import.ps1` はブックを書き換える。引数を間違えたときの既定の挙動が「書き換え」だと
取り返しがつかない。既定を下見にしておけば、間違った引数で走らせても
「何が変わるはずだったか」が出るだけで済む。

バックアップは併用するが、バックアップは**事故が起きた後**の話であって、
**事故を起こさない**ための仕組みではない。両方要る。

## ★ 設計原則 97 — 往復するツールの受け入れ条件は「2 回目で差分が出ないこと」にする ★

1 回目の書き込みが成功しても、往復が閉じている保証にはならない。
E-2 では `AddFromString` が末尾に改行を足すため、**1 回目は成功するのに
繰り返すたびに空行が増える**という壊れ方をしていた。

冪等性を受け入れ条件に据えると、こういう「片道では見えない」ずれが必ず露出する。
検証は往路・復路の両方向から掛ける (同じ入力でもう一度 / 出力を入力に戻して)。

### 確立した仕様事実 (39〜40)

## ★ 仕様事実 39 — `CodeModule.Lines` はメンバー属性行を返さないが `Export` は書き出す ★

`Attribute m_browser.VB_VarHelpID = -1` のようなメンバー属性行は、
`WithEvents` メンバの宣言直後に置かれる。

| 経路 | 属性行 |
|---|---|
| `CodeModule.Lines` | **含まれない** (VBE が隠す) |
| `VBComponent.Export` | **書き出される** |
| `CodeModule.AddFromString` | ~~受け付けて、メンバー属性として保存する~~ |

★**最後の行は誤りだった。K-2 で訂正した。**★
`AddFromString` は属性行を**ただのテキストとして挿入する**だけで、VBE は別途
自分で `VB_VarHelpID` を生成する。結果 **同じ属性行が 2 本になり、片方が
VBE のコードペインに露出してコンパイルエラーになる。**

E-2 でこれを見抜けなかった理由も分かっている。**`CodeModule.Lines` は
Attribute 行を一切返さない**ので、COM 経由の照合では重複が見えない。
`Export` も重複を畳んで 1 本しか書き出さないため、往復照合でも見えない。
★ブック内部のモジュールストリームを直接読まないと分からない★
(`tools/check_book.py` を新設した)。

したがってエクスポートしたファイルと `CodeModule` の内容は**行数が一致しない。**
現行ブックでは `Wv2Browser` 4 件 / `Wv2NavBar` 1 件 / `Wv2TabBar` 1 件。

## ★ 仕様事実 40 — `AddFromString` は末尾に改行を 1 つ足す ★

渡した文字列の末尾に改行が 1 つ追加される。末尾の空行を落とさずに渡すと、
import のたびに空行が 1 行ずつ増え続ける。

また **書き込み直後の `CountOfLines` は保存後の値と食い違う** (実測で 1047 と 1046)。
行数で検証するなら保存して開き直した後に読むこと。

### E-3 以降への申し送り

- **新規モジュールの追加は未対応。** `Wv2Log.bas` を新設する K-1 では、
  先に VBE で空の標準モジュールを作る必要がある。詰まるようなら
  `VBComponents.Add` の実装を E-3 として切る。
- **`forms/` と `src/document/` の更新手順は依然として手動。**
- 化けた 76 文字 (仕様事実35) の置換は、`import.ps1` が動くようになったので
  **機械的に実行できるようになった。** 事前ゲートが通る代替字に置き換えて import すればよい。

---

## E-3 段階 — 新規モジュールの追加 (`import.ps1` の `-AllowNew`)

### 位置づけ

E-2 の `import.ps1` は**既存コンポーネントの中身を差し替えるだけ**で、`src/` にあって
ブックに無いモジュールは飛ばしていた。K-1 で `Wv2Log.bas` を新設するため、
「VBE で空のモジュールを手で作ってから import する」という手順が残っていた。これを消す回。

**製品コードの変更はゼロ。** 検証はすべてブックの複製に対して行い、
`book/WebView2_v0.5.2.xlsm` は最初から最後まで md5 が変わっていない
(`FCF5E0D94B6D242371DFDA00EBF45C56`)。

### ★初手の差し替え★ `VBComponents.Add` ではなく `Import` にした

たーぼーさんの指示は `VBComponents.Add` の実装だったが、事前調査を踏まえて
**`Import` を使う案を提示し、合意のうえ差し替えた** (第9.30 / D-1 の教訓)。

| 案 | 内容 | 評価 |
|---|---|---|
| A | `VBComponents.Add` + 改名 + `AddFromString` | **ヘッダー属性を設定する手段が無い。** 今は全クラスが既定値なので実害ゼロだが、将来 `VB_PredeclaredId = True` のクラスを作ると**黙って違うものができる** |
| **B (採用)** | 一時ファイルを **CP932 + CRLF** で書いて `VBComponents.Import` | 新規作成の正規 API。**ヘッダーの属性も名前も自動で正しく付く** |

E-1 / E-2 で `Import` を避けていたのは「**`Remove` + `Import`**」の組み合わせの罠
(削除が遅延して同名 Import が `Wv2Pane1` に化ける) であって、**`Remove` しない
新規追加には当たらない。** 既存の差し替えは今までどおり `AddFromString` のままで、
**新規のときだけ `Import`** というハイブリッドにした。

事前調査で、現行の 9 クラスは**全部ヘッダー属性が既定値**
(`PredeclaredId=False` / `GlobalNameSpace=False` / `Creatable=False` / `Exposed=False` /
`MultiUse=-1`) であることを確認済み。`.bas` の `Attribute VB_Name` も全ファイルで
ファイル名と一致していた。

### 論点と決定 (7 件を一括合意)

| # | 論点 | 決定 |
|---|---|---|
| 1 | 新規追加の手段 | **CP932 + CRLF の一時ファイル + `Import`** (上記) |
| 2 | 明示フラグ | **`-AllowNew` を必須にする。** `src/` にゴミを置いたとき勝手に増えるのを防ぐ (設計原則96 の延長) |
| 3 | 対象種別 | 標準 `.bas` とクラス `.cls` のみ |
| 4 | ヘッダー属性の検証 | **新規・既存を問わず照合し、食い違えば警告。** `import.ps1` は本体しか比較していなかったので、属性のズレを見逃す穴があった |
| 5 | `VB_Name` とファイル名 | **不一致なら中止。** `Import` は `VB_Name` で名前が決まる |
| 6 | 失敗時 | バックアップからの復旧を案内して終了コード 1 |
| 7 | ついでの実験 | `Import` が BOM なし UTF-8 を受けるかを確認し、仕様事実36 の未確認部分を閉じる |

### ★踏んだ壁 1: 一時ファイルを LF で書くとクラスが標準モジュールになる★

最初の書き込み試験で、`.bas` は成功したのに `.cls` が**標準モジュールとして作られた。**
本体は 12 行のはずが 16 行になり、ヘッダーがコードに落ちていた:

```
  1| Attribute VB_Name = "E3TestCls"
  2| VERSION 1.0 CLASS
  3| BEGIN
  4|   MultiUse = -1  'True
  5| End                      <- VBA が大小を正規化した痕跡。コードとして解釈された証拠
  6| ' --- E3TestCls.cls ...
```

ヘッダーのバイト列は本物の `.cls` と**完全に一致**していたので、原因は中身ではなかった。
真因は **`Split-ModuleFile` が改行を LF に正規化しており、その文字列をそのまま
CP932 で書き出していた**こと。`Import` は **CRLF でないとヘッダーブロック
(`VERSION 1.0 CLASS` / `BEGIN..END`) を認識せず**、標準モジュールとして扱う。

一時ファイルを CRLF で書くようにして解決した。併せて**追加後に種別も照合する**ようにした
(名前だけ見ていると、この壊れ方を素通しする)。

### ★踏んだ壁 2: 新規追加が「触っていないモジュール」を書き換える★

試験用クラスに `Public Property Get N() As Long` と**大文字の `N`** を書いたところ、
**VBA がプロジェクト全体の `n` を `N` に書き換えた。**
`Wv2Browser` / `Wv2Pane` / `Wv2TabBar` / `Wv2Thunks` の計 70 行が、
**import では一切触っていないのに**変化した。

```
-    Dim n As Long
-    n = m_tabs.count
+    Dim N As Long
+    N = m_tabs.count
```

仕様事実38 (識別子の大小統一) は D-2 / E-1 で記録済みだったが、
**「新規モジュールの追加が他の全モジュールを書き換える」**という牙は書いていなかった。
動作は変わらない (VBA は大小を区別しない) が、**`git diff` が汚れて本当の変更を覆い隠す。**

### ★踏んだ壁 3: 自前のドリフト検査が静かに嘘をついた★

壁 2 を検出するため、保存後にブックを開き直して照合する検査を書いた。
ところが**実際にドリフトしているのに「ドリフトなし」と報告した。**

- 同じ Excel インスタンスで閉じて開き直す → 見えない
- Excel を終了させ、**同じ PowerShell プロセスから**新しい COM インスタンスを作る → 見えない
- **別プロセスの `export.ps1`** → 見える

★この正規化は、import を行ったプロセスからは観測できない。★
自前比較をやめ、**別プロセスで `export.ps1` を呼んでバイト照合する**方式に替えた。
既に検証済みのコードを再利用でき、大小統一だけでなく末尾空行やヘッダー属性の
ズレも同じ仕掛けで捕まる。

**発火しない検査器は、無い検査器より悪い** (設計原則95)。
「ドリフトなし」という嘘を一度も疑わずに通していたら、この壁は本番で踏んでいた。

### 実機検証の結果 (全合格)

**すべてブックの複製に対して実施。** 本物のブックは前後で md5 不変。

| # | 検証 | 結果 |
|---|---|---|
| 1 | `-AllowNew` 無しで下見 | **拒否。** 「ブックに無い (-AllowNew が要る)」と報告して追加しない |
| 2 | `-AllowNew -Apply` | **`.bas` と `.cls` の 2 本を追加。** 照合 OK |
| 3 | export し直して種別と中身を確認 | **`.cls` はクラスモジュール、`.bas` は標準モジュール。** バイト数も原本と一致 (459 / 318)。日本語と `★ ← → ⇔ ① ～ ―` も無傷 |
| 4 | ドリフト検出 | **`Wv2Browser` / `Wv2Pane` / `Wv2TabBar` / `Wv2Thunks` の 4 本を正しく列挙** |
| 5 | ★収束★ ドリフトを `src/` に取り込んで下見 | **0 / 17。** 往復が収束する |
| 6 | `VB_Name` 不一致 | **終了コード 1 で中止。** ブックは無変更 |
| 7 | ヘッダー属性の食い違い (`PredeclaredId=True`) | **本体が同一でも検出。** 「src=True / ブック=False」と報告 |
| 8 | `Import` に BOM なし UTF-8 を渡す | **文字化けした** (下記) |
| 9 | 本物のブックに下見 (回帰) | **0 / 15。** E-2 と同じ |

### 論点7 の実験 — `Import` は BOM なし UTF-8 を受け付けない

BOM なし UTF-8 の `.bas` を直接 `Import` して読み戻したところ、典型的な文字化けになった。

```
渡した   : ' UTF-8 BOM なしで Import した場合に日本語が無傷か: 日本語テスト ★①～
読み戻し : ' UTF-8 BOM 縺ｪ縺励〒 Import 縺励◆蝣ｴ蜷医↓譌･譛ｬ隱槭′辟｡蛯ｷ縺・ ...
```

**`VBComponents.Import` はファイルを ANSI コードページ (CP932) として読む。**
これで仕様事実36 の未確認部分が閉じた。同時に、**一時ファイルを CP932 で書く設計は
「念のため」ではなく必須だった**ことが確定した。

※ なお、チャット時代にたーぼーさんが Claude の出力した UTF-8 ファイルを取り込んで
   日本語が無事だった経緯は**未解明**。VBE の GUI (ファイル → ファイルのインポート) が
   プログラム的な `Import` と違う判定をしている可能性、あるいはコードの貼り付けで
   運用していた可能性がある。**現在の往復には影響しない**ので追わない。

### 実装上の事後報告 (設計原則79)

1. **`-SkipDriftCheck` を追加した。** ドリフト検査は export を 1 回余分に走らせるので、
   急ぐときに外せるようにした。通常運用では使わない。
2. **追加後に「種別」も照合するようにした。** 壁 1 は名前だけ見ていると素通しする。
3. **ドリフト検査が実行できなかったときに警告を出すようにした。**
   最初の実装は `if ($null -ne $wb2)` で**静かに飛ばす**作りで、
   これが壁 3 の発見を遅らせた。**失敗を黙って飛ばす分岐を作らない。**

### 変更ファイル

| ファイル | 内容 |
|---|---|
| `tools/import.ps1` | `-AllowNew` / `-SkipHeaderCheck` / `-SkipDriftCheck` を追加。新規追加・ヘッダー属性照合・ドリフト検査 |
| `CLAUDE.md` | 往復の説明を更新 |

**`Wv2*` の製品コードは 1 行も変えていない。**

### 確立した設計原則 (98)

## ★ 設計原則 98 — 検証は本番と同じ経路で行う (同一プロセスの観測はキャッシュで嘘をつく) ★

壁 3 で踏んだ。VBA の識別子正規化は、import を行った PowerShell プロセスからは
**Excel を終了させて新しい COM インスタンスを作っても観測できなかった。**
別プロセスで開いて初めて実態が見える。

自前で「読み直して比べる」処理を書くと、こういうキャッシュの層をすり抜けられずに
**正しそうな嘘**を返す。既に本番経路で動いているツール (ここでは `export.ps1`) を
そのまま子プロセスとして呼ぶ方が、短く書けて、しかも正しい。

### 確立した仕様事実 (41〜43)

## ★ 仕様事実 41 — `VBComponents.Import` はファイルを ANSI コードページとして読む ★

BOM なし UTF-8 を渡すと日本語が文字化けする (実測)。
`import.ps1` は一時ファイルを **CP932** で書いてから `Import` に渡している。
CP932 で表現できるかは事前ゲート (`check_cp932.py`) が保証する。

## ★ 仕様事実 42 — `Import` に渡すファイルは CRLF でなければならない ★

LF だけで書くと `VERSION 1.0 CLASS` / `BEGIN..END` のヘッダーブロックが認識されず、
**クラスモジュールが標準モジュールとして作られ、ヘッダーがコードに落ちる。**
ヘッダーのバイト列が正しくても起きるので、改行コードを疑うこと。

## ★ 仕様事実 43 — 新規モジュールの追加はプロジェクト全体の識別子の綴りを書き換える ★

仕様事実38 の続き。新規モジュールが既存と同じ綴りの識別子を別の大小で宣言していると、
**コンパイル時に「触っていないモジュール」の中身まで書き換わる。**
E-3 の実測では、試験用クラスの `Property Get N()` により
`Wv2Browser` / `Wv2Pane` / `Wv2TabBar` / `Wv2Thunks` の計 70 行が `n` → `N` に変化した。

★**この正規化は import を行ったプロセスからは観測できない**★
Excel を終了させて新しい COM インスタンスを作っても正規化前の綴りが返る。
別プロセスで開く必要がある (設計原則98)。

動作は変わらないが `git diff` が汚れるので、**`import.ps1` は別プロセスの
`export.ps1` で照合して報告する。** 報告が出たら `export.ps1 -Force` で `src/` に取り込む。

### E-4 以降への申し送り

- **`forms/` と `src/document/` の更新は依然として手動。**
- 新規モジュールを足すときは、**既存と同じ綴りの識別子を別の大小で宣言しない**よう
  気を付けると壁 2 を避けられる。避けられなくても検出はされる。
- 化けた 76 文字 (仕様事実35) の置換は、これで完全に機械化できる状態になった。
