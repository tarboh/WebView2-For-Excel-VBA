# VBA-WebView2 開発メモ ― N 軸 (ネットワーク調査)

★2026-08-25 新設。★ 索引は `開発メモ_0_索引.md`。

---
## N 軸とは何か

★**ブラウザが実際に何を叩いているかを、Excel VBA だけで見る。**★
F12 の Network タブに相当するものを、外部ライブラリゼロで作る。

### なぜコンセプトに合うのか

コンセプト① (Web ページを自動制御することに優れた使いやすさ) の ★前段★ は、
必ず「何を叩けばよいかを調べる」である。そこが一番面倒だという実務からの声が
発端になった (2026-08-25 たーぼーさん):

> 基幹システムを操作して CSV を吐かせ、それを PowerShell で自動化した。
> HTTP で POST したりレスポンスを読むために ★F12 の Network タブのログを調査する
> 部分が非常に面倒★ だった。必要な部分だけ抽出できる調査ツールが作れないか。

★出口まで含めると、F12 に無い価値になる★ ―― キャプチャした要求を
**PowerShell の `Invoke-WebRequest` コードとして吐く**。「ブラウザで 1 回手で操作 →
その通信を PowerShell コードで受け取る → タスクスケジューラに載せる」という
実務の流れが、そのまま道具になる (`Copy as cURL` はあっても PowerShell 版は無い)。

### ★方式の選択 (決定済み)★

| | A. ページ内 JS のラップを拡張 | B. WebView2 のネイティブイベント |
|---|---|---|
| 実装 | 軽い (D-4 のプローブに足すだけ) | 重い (COM 経路の新規配線 + `IStream`) |
| `fetch` / `XHR` | 見える | 見える |
| ★フォーム POST でのページ遷移★ | ★**見えない**★ | ★見える★ |
| 画像 / CSS / リダイレクト | 見えない | 見える |

★**B で確定**★。対象の基幹システムが ★フォーム POST 中心 (古典的)★ で、
A ではまったく見えないため。

### 段階の切り方

| 段階 | 内容 |
|---|---|
| **N-1** | `WebResourceRequested` を配線し、★URL + メソッド + 種別の一覧★ |
| N-2 | リクエストの**ヘッダとボディ** (`IStream` 読み出し) |
| N-3 | レスポンスの**ステータスとヘッダ** (`WebResourceResponseReceived`) |
| N-4 | レスポンス**本文** |
| N-5 | ★**PowerShell の `Invoke-WebRequest` コードとして吐く**★ |

★N-1 だけでも実用になる★ ―「手で 1 回操作して、何をどの順で叩いているか」が
見えれば、F12 で一番時間を食う部分が片付く。

---
## N-1 段階 ― WebResourceRequested の配線 (完了)

### 論点と決定 (8 件を一括合意)

| # | 論点 | 決定 |
|---|---|---|
| 1 | 軸と分冊 | ★N 軸・本ファイルを新設★ |
| 2 | 方式 | ネイティブイベント (上記) |
| 3 | N-1 のスコープ | ★URL / メソッド / 種別だけ★。ヘッダもボディもレスポンスも後段 |
| 4 | フィルタの既定 | ★Document / XHR / Fetch に絞る★ (画像・CSS・フォントは除外。広げる口は用意) |
| 5 | ハンドラの作法 | ★記録だけしてすぐ返す★ (下記) |
| 6 | 記録の置き場 | ★メモリ上のリングバッファ + 取り出し API★ (D-4 の `SpaProbeDrainLog` と同じ型) |
| 7 | 出口 | N-1 はログのみ。シートやコード生成は後段 |
| 8 | 検証 | ★自前 HTML でフォーム POST を作る★ (実サイトに頼らず、まず確実に捕まることを見る) |

★論点5 の理由★ `WebResourceRequested` は「要求を止める / 書き換える」ための
イベントで、★ハンドラが遅いと通信そのものが詰まる★。仕様事実14 / 20 の領域でもあり、
**ハンドラの中で `EvalSync` を呼んではいけない**。記録だけしてすぐ返す。

★論点6 の理由★ 要求は 1 ページで数十〜数百件出る。ログに直書きすると量で溢れる
(D-4 で `SpaProbeDrainLog` を作ったときと同じ判断)。

### ★調査: vtable index を確定させた★

★**COM の vtable index を推測で書くとクラッシュする。**★
既存の配線 (`remove_NavigationStarting = 8` など 6 本) から順番を逆算して
`add_WebResourceRequested = 54` と推測したが、★**正解は 55 だった**★。
`OpenDevToolsWindow` (51) を数え落としていた。
**実機で試していたら、1 つずれた別のメソッドを呼んでクラッシュしていた。**

そこで ★SDK のヘッダ (`WebView2.h`) から機械的に抜く道具★ を作った:

```bash
python tools/vtable.py ICoreWebView2
python tools/vtable.py --enum COREWEBVIEW2_WEB_RESOURCE_CONTEXT
python tools/vtable.py --find WebResource
```

★検算★ `remove_DOMContentLoaded = 65` が既存の `RemoveVtableIndexFor` の値と
一致することを確認済み。道具の正しさはこれで裏付けた。

★ヘッダの場所★ `C:\Users\gugug\Downloads\新しいフォルダー (4)\WebView2.h`
(2.7 MB)。★Downloads の中なので消えうる★。環境変数 `WEBVIEW2_H` でも指定でき、
`tools/WebView2.h` に置いても拾う。**消えたら SDK から取り直すこと。**

#### N-1 で使う番号 (確定)

| 対象 | index |
|---|---|
| `ICoreWebView2::add_WebResourceRequested` | ★**55**★ |
| `ICoreWebView2::remove_WebResourceRequested` | 56 |
| `ICoreWebView2::AddWebResourceRequestedFilter` | ★**57**★ |
| `ICoreWebView2::RemoveWebResourceRequestedFilter` | 58 |
| `...WebResourceRequestedEventArgs::get_Request` | 3 |
| `...WebResourceRequestedEventArgs::get_ResourceContext` | 7 |
| `ICoreWebView2WebResourceRequest::get_Uri` | 3 |
| `ICoreWebView2WebResourceRequest::get_Method` | 5 |

#### N-2 以降で使う番号 (先に控えておく)

| 対象 | index |
|---|---|
| `ICoreWebView2WebResourceRequest::get_Content` (IStream) | 7 |
| `ICoreWebView2WebResourceRequest::get_Headers` | 9 |
| `ICoreWebView2HttpRequestHeaders::GetIterator` | 8 |
| `...HttpHeadersCollectionIterator::GetCurrentHeader` | 3 |
| `...HttpHeadersCollectionIterator::get_HasCurrentHeader` | 4 |
| `...HttpHeadersCollectionIterator::MoveNext` | 5 |
| `ICoreWebView2_2::add_WebResourceResponseReceived` | 61 |

#### `COREWEBVIEW2_WEB_RESOURCE_CONTEXT` (フィルタの引数)

| 値 | 意味 | N-1 の既定 |
|---|---|---|
| 0 | ALL | ― |
| ★1★ | DOCUMENT | ★入れる★ (フォーム POST / ページ遷移) |
| 2 / 3 / 5 | STYLESHEET / IMAGE / FONT | 除外 |
| 6 | SCRIPT | 除外 |
| ★7★ | XML_HTTP_REQUEST | ★入れる★ |
| ★8★ | FETCH | ★入れる★ |
| 11 | WEBSOCKET | 除外 (N-1 では扱わない) |

★フィルタを 1 つも登録しないとイベントは 1 件も来ない★ ので、
`AddWebResourceRequestedFilter("*", ctx)` を 3 回呼ぶことになる。

### 確立した設計原則 (115) ― 準備の段階で

- **設計原則115 (N-1) COM の vtable index は推測で書かず、SDK のヘッダで確定させる。**
  設計原則107 (外部の対象を掴む識別子は推測で書かない) の COM 版だが、
  ★危険度が違う★ ―― セレクタが外れても「見つからない」で済むが、
  ★vtable index が 1 つずれると別のメソッドを呼んでプロセスごと落ちる★。
  既存の配線からの逆算は**検算にしか使わない**こと。実際 N-1 の準備で
  逆算は 1 つずれていた。道具は `tools/vtable.py`。

### 実装 (差し替え 4 本・新規モジュールなし)

| ファイル | 内容 |
|---|---|
| `Wv2Thunks.bas` | `HK_WebResourceRequested = 11` / `m_iidTable` の上限拡張 / IID 追加 |
| `ComCallbackHandler.cls` | `Case HK_WebResourceRequested` → `View_OnWebResourceRequested` |
| `Wv2Pane.cls` | 使う側の口 + COM 配線 4 本 + 受け口 + リングバッファ |
| `Wv2Tests.bas` | `Test_N1_*` 6 本 + 自前 HTML |

IID は `WebView2.h` の `MIDL_INTERFACE` 行から採った (設計原則115):
`ICoreWebView2WebResourceRequestedEventHandler` = `{ab00b74c-15f1-4646-80e8-e76341d25d71}`

#### 使う側の口

```vba
p.NetCaptureStart          ' ★Navigate の前に呼べば初回の DOCUMENT から捕まる★
' ... 手で 1 回操作する ...
p.NetLogDrain              ' 溜まった分をログへ流して空にする
p.NetCaptureStop           ' フィルタも外す
```

| メンバ | 意味 |
|---|---|
| `NetCaptureStart(contexts, uriFilter)` | 捕捉開始。既定は `DOCUMENT,XHR,FETCH` / `"*"` |
| `NetCaptureStop()` | 捕捉終了。★張ったフィルタも外す★ |
| `NetCaptureOn` | 捕捉中か |
| `NetLogDrain()` | 溜まった分を `Wv2Log` へ流して★中身だけ★空にする |
| `NetLogLine(i)` | 流さずに 1 件読む (古い順・1 起点・`vbTab` 区切り 5 項目) |
| `NetLogCount / NetLogTotal / NetLogDropped` | 手元 / 通算の発火 / 通算の溢れ |
| `NetLogCapacity` | リングの容量 (既定 500)。変えると中身は作り直す |
| `NetLogClear()` | 通算も含めて全部 0 に戻す |

低い層は `View_AddWebResourceRequested` / `View_RemoveWebResourceRequested` /
`View_AddWebResourceRequestedFilter` / `View_RemoveWebResourceRequestedFilter` として
公開してある (種別を細かく制御したいとき用)。

#### ★記録だけしてすぐ返す (論点5) の徹底★

受け口 (`View_OnWebResourceRequested`) の中では

- `Wv2Log` も `Debug.Print` も撃たない ―― ★他の `Case` にある 4 行の
  `Debug.Print` を、この種別だけ意図的に外した★
- `dcf` / `GetStringProperty` に渡す `funcName` も空にして、失敗しても
  イミディエイトに何も出さない (失敗はリングに `(err)` として書き残す)
- ★エラーを再送出しない★ ―― 他の受け口は `Err.Raise` するが、通信の最中に
  エラーダイアログが出ると操作が続けられない。握り潰した事実は
  `NetLogDrain` の出力に必ず現れる

`get_Request` は AddRef 済みで返るので必ず `ComRelease` する。1 ページで数百回
通る経路なので、ここを漏らすと目に見えて漏れる。

#### 経過 ms の時刻源に `GetTickCount` を使った

★`Timer` は使えない★ ―― `Timer` は Single の「午前 0 時からの秒」なので、
午後の値 (5 万秒台) では Single の有効桁が足りず ms の分解能が消える。
さらに日付を跨ぐと巻き戻る。`GetTickCount` は起動からの ms で単調増加。
戻り値は Long なので約 49.7 日で一周するが、差を Double に上げて計算し、
負になったら 2^32 を足して戻している (`NetElapsedMs`)。

---

### 実装前の検算 (実機に出す前に潰したもの)

| 検算 | 結果 |
|---|---|
| vtable index を `tools/vtable.py` で再確認 | 55/56/57/58、args 3/7、request 3/5、enum 全 17 値 |
| リングバッファを Python で総当たり | 容量 1〜8 × 件数 0〜30 を `deque` と突き合わせ **502 件一致** |
| 検証ページを VBA ソースから組み立て直して実 DOM で確認 | Edge headless で 15 件 OK |

★アルゴリズムは Python で先に検証できる★ (D-1 の `JsonPickStr`、D-2 の
`JsonUnescape` と同じ手)。リングの添字計算は VBA で書いてから間違いに気づくと
実機 1 往復を丸ごと失うので、先に総当たりで潰した。

---

### 実機検証 ― ★3 回かかった★

#### 1 回目: 判定 24 件 OK 14 / FAIL 10 (総発火 0 件)

**イベントが 1 度も来なかった。** `add_WebResourceRequested` も
`AddWebResourceRequestedFilter` × 3 も S_OK なのに、`Invoke` が 0 回。

★OK 14 件のうち 4 件は空振り★ だった (「`probe.css` が 1 件も居ない」
「止めた後は 1 件も来ない」など)。何も捕まっていないので自動的に通っただけで、
証拠になっていない。

**★テストの設計に穴があった★** ―― 検証ページの通信が
**1 本残らず仮想ホスト経由**だった。初回の DOCUMENT も、`data.json` への
fetch / XHR も、`echo.html` への POST も、すべて `https://appassets.netprobe/`。
つまりこのログは

- (a) 配線が効いていない (vtable / IID / フィルタ)
- (b) 仮想ホストの要求だけがイベントに乗らない

を**まったく区別できない**。「実サイトに頼らない」(論点8) を優先して
対照を 1 本も入れなかったのが原因で、**プローブ自体を検算しきれていなかった**
(設計原則103 と同じ失敗の形)。

★切り分け用に用意しておいた `Test_N1_Site` が効いた★ ―― 落ちたときに
自動でその案内をログに出すようにしてあったので、次の一手で迷わなかった。

#### 1 回目 (b): `Test_N1_Site` ― 判定 4 件 OK 4 / FAIL 0

実サイト (Google) では **10 件すべて捕まった**。

```
#   経過ms  メソッド 種別       URL
1       94  GET      DOCUMENT   https://www.google.com/
2      141  GET      DOCUMENT   https://www.google.com/
3      609  GET      XHR        .../async/hpba?...
4      672  OPTIONS  XHR        https://ogads-pa.clients6.google.com/$rpc/...
5      906  GET      XHR        .../xjs/_/js/...
6      953  GET      XHR        .../complete/s?q&...
7     1016  GET      XHR        .../async/hpba?...
8     1062  POST     XHR        https://ogads-pa.clients6.google.com/$rpc/...
9     1500  OPTIONS  XHR        https://play.google.com/log?...
10    1687  POST     XHR        https://play.google.com/log?...
```

- メソッドが正しく取れている (GET / POST / **OPTIONS** = CORS プリフライトまで)
- 種別が正しく分かれている
- `uri="*"` が効いている (`google.com` 以外の別ホストも拾えている)
- 画像・CSS・スクリプトが 1 件も居ない (既定フィルタが効いている)

**★配線は最初から正しかった。★** 原因は (b) で確定した ―― これが仕様事実69。

#### 2 回目 (N-1b): 判定 34 件 OK 31 / FAIL 3

的を 2 系統 + 対照に組み直した (論点1 案G / 論点2 案a)。

| | 的 | 性質 |
|---|---|---|
| **案F** | `http://127.0.0.1:59999/…` | 到達不能。★外部依存ゼロ★ |
| **案D** | `https://httpbingo.org/…` | ネットが要る代わりに素直に届く |
| **対照** | 仮想ホストの `data.json` | ★捕まらないことを数える★ |

★案F の 2 つの落とし穴を先に潰した★

- **ポート番号** ―― Chromium が塞いでいるポート (1/7/9/11/13/…/10080) を避ける。
  塞がれたポートだと要求が発行される前に止まる恐れがある。59999 は塞がれていない
- **混在コンテンツ** ―― `127.0.0.1` と `localhost` は仕様上
  「潜在的に信頼できるオリジン」なので、https のページから http で呼んでも
  混在コンテンツ扱いされない。★`.invalid` のような名前を使うとここで詰まる★

★**N-1 の本丸が取れた**★

```
1  188  POST  DOCUMENT  http://127.0.0.1:59999/echo      ← フォーム POST でのページ遷移
1  421  POST  DOCUMENT  https://httpbingo.org/post       ← 同上 (外部)
1  188  GET   DOCUMENT  http://127.0.0.1:59999/n1-doc-get
1  203  GET   DOCUMENT  https://httpbingo.org/get?k=doc
```

**ページ内 JS のラップでは絶対に見えない経路** (フォーム POST での遷移) が、
両系統で捕まった。方式 B を選んだ理由がここで回収できた。

残った FAIL 3 件は全部 fetch の判定だった。(4) のドレインを見ると

```
8   500  GET  XHR  https://httpbingo.org/get?n=3
9   562  GET  XHR  http://127.0.0.1:59999/n1?n=4
10  609  GET  XHR  https://httpbingo.org/get?n=4
```

**`fetch()` で撃った 10 本が全部 `XHR` 種別で届いている。**
テストが「fetch なら種別は FETCH のはず」と決めつけていたので落ちただけで、
★捕まってはいた★。これが仕様事実70。

#### 3 回目 (N-1c): 判定 35 件 OK 35 / FAIL 0

テスト側だけ直した (実装は無傷)。

1. **種別の決めつけをやめた** ―― 判定は「捕まったか」で行う。
   連動して `localOk` / `netOk` の誤報 (「1 件も届いていない」) も消えた
2. ★**「fetch は XHR 種別で届く」を数える判定を足した**★ ―― 見つけた仕様事実を
   そのまま回帰試験にする。観察した種別を書き出す道具 `N1CtxOf` を用意し、
   ★決めつけを潰したのに新しい決めつけを入れない★ようにした
3. **(3b) にドレインを 1 回挟んだ** ―― 2 回目では (4) で容量を 3 に落とした時点で
   (2) の記録が消え、★「seq 1/2 は f-local と f-net だったはず」と推測で埋める
   羽目になった★。消える前に必ず出す

```
届いた種別: f-local=XHR  x-local=XHR  f-net=XHR  x-net=XHR
--- ネットワーク要求 4 件 (総発火 4 件) ---
  1  3437  GET  XHR  http://127.0.0.1:59999/n1?k=f-local
  2  3500  GET  XHR  https://httpbingo.org/get?k=f-net
  3  3547  GET  XHR  http://127.0.0.1:59999/n1?k=x-local
  4  3609  GET  XHR  https://httpbingo.org/get?k=x-net
```

seq 1 / 2 が実在したことで、2 回目の推測が記録として確定した。

---

### 途中で直したもの ― ★数字が嘘に見える行★ (論点3 案Y)

2 回目のログの最後がこうなっていた。

```
Wv2Pane.NetCaptureStop: 捕捉終了 (総発火 0 件 / 手元 0 件 / 溢れ 0 件)
```

**実際には 10 件来ている。** `NetLogDrain` が中身と一緒に通算カウンタも
0 に戻していたためで、この行だけ見た人は「1 件も来なかった」と読む。
`NetLogTotal` は「捕捉開始からの総発火数」と説明しているので、説明と実装が
食い違っていた。

- `NetLogDrain` … リングの中身だけ空にする。★通算は保つ★
- `NetLogClear` … 通算も含めて全部リセット
- `NetCaptureStart` … `NetLogClear` を通るので毎回 0 から数え直す

再発防止の判定も入れた (`★ドレインしても総発火は残る (通算)★`)。

---

### 確立した設計原則 (116 / 117)

- **設計原則116 (N-1) 自前で閉じた検証は、閉じた世界そのものが原因のときに
  何も言えない。★対照を必ず 1 本混ぜる★。**
  N-1 の 1 回目は「実サイトに頼らない」を優先して的を全部仮想ホストに置いた
  結果、全滅したときに「配線が悪い」のか「仮想ホストが特殊」なのかを
  区別できなかった。★閉じた検証ほど、閉じていない対照が要る★。
  設計原則103 (プローブ自体を検算する) の系。

- **設計原則117 (N-1) 相手が返す分類を自分の言葉で決めつけない。
  ★観察した値をログに書き出してから判定する★。**
  `fetch()` で撃ったのだから種別は `FETCH` だろう、という決めつけで
  FAIL 3 件を出した。実際は `XHR` で届く。分類・列挙・状態名のように
  ★相手が決める語彙★ を条件に使うときは、まず実測値を出力して確かめる。
  設計原則112 (間接的な指標を使わない) の語彙版。

### 確立した仕様事実 (69 / 70 / 71)

- **仕様事実69** ★`SetVirtualHostNameToFolderMapping` で配信した要求は
  `WebResourceRequested` に乗らない★ (DOCUMENT / fetch / XHR / 画像 / CSS のすべて)。
  ランタイムが内部の資材ハンドラで直接返しているため。★配信自体は問題なくできる。
  乗らないのはイベントだけ★ なので、検証ページは仮想ホストのままでよく、
  **的だけを外に出せばよい**。

- **仕様事実70** ★JS の `fetch()` の要求は `FETCH`(8) ではなく
  `XML_HTTP_REQUEST`(7) として届く★。実機で fetch 10 本を撃って 10 本とも XHR。
  種別で絞り込むときに決めつけないこと。既定のフィルタは `FETCH`(8) も
  張ったままにしてある (害はなく、将来変わっても拾える)。

- **仕様事実71** ★`http://127.0.0.1:<port>` は https のページから呼んでも
  混在コンテンツで止まらない★ (仕様上「潜在的に信頼できるオリジン」)。
  ただし ★Chromium が塞いでいるポート (1/7/9/11/13/…/10080 など) は避ける★。
  この 2 つを踏まえれば、★到達不能なローカルアドレスは「外部依存ゼロで
  ネットワーク要求を発生させる的」として使える★ (接続は失敗するが要求は飛ぶ)。

---

### N-1 で使えるようになったもの

★**手で 1 回操作して、何をどの順で叩いているかが見える。**★

```
Test_N1_Watch          ' 今開いているタブの通信を捕まえ始める
Test_N1_Watch "ALL"    ' 画像や CSS まで見たいとき
' ... ページを手で操作する (ログインする、検索する、CSV を吐かせる…)
Test_N1_Drain          ' 溜まった分をログへ流して空にする
Test_N1_Stop           ' 終わり (フィルタも外れる)
```

回帰試験は `Test_N1_Capture` (35 件)、切り分けは `Test_N1_Site`、手順は `Test_N1_Help`。

### 次にやること (→ N-2 で消化した)

- ~~**N-2** リクエストの**ヘッダとボディ**~~ → ★完了 (下記 N-2 段階)★
- 宿題: ★`FETCH`(8) のフィルタは実際には使われていない可能性がある★
  (仕様事実70)。張らなくても fetch が捕まるかは未確認。害がないので当面そのまま

---
## N-2 段階 ― リクエストのヘッダとボディ (完了)

N-1 の一覧 (URL / メソッド / 種別) の上に、★1 本を再現するのに要るもの★を足す。

### 論点と決定 (9 件を一括合意)

| # | 論点 | 決定 |
|---|---|---|
| 1 | ボディをいつ読むか | ★案A: ハンドラ内で上限まで読む★ |
| 2 | 読んだ後どう戻すか | ★案X: `Seek(0)` で先頭へ戻す★ |
| 3 | `Cookie` / `Authorization` | ★案M: 既定で伏せる (明示的に解除可)★ |
| 4 | ボディの表現 | 案P: `Content-Type` を見てテキストなら UTF-8、他はバイト数 + 先頭 hex |
| 5 | 記録の持ち方 | ★案S: 詳細は別リング (既定 50 件)。一覧は 500 件のまま★ |
| 6 | 詳細の取り出し | 案V: 一覧は 1 行のまま、`NetDetail(i)` で 1 本だけ出す |
| 7 | ボディを読む対象 | 案α: 既定は GET 以外だけ (+ URL で絞る口も用意) |
| 8 | 詳細取得の既定 | ★案κ: 詳細はまるごと既定 OFF★ |
| 9 | 「壊れていない」の確かめ方 | ★案D': httpbingo が返す JSON と突き合わせる★ |

★論点1 と論点2 が N-2 の中身そのもの★ ―― 他は運用の作法。

**論点1 の理由** `args` は Invoke の間しか有効でないので、★ボディはハンドラの中で
同期的に読むしかない★。案B (`GetDeferral` で後回し) は Deferral を握っている間
その要求がブロックされ続けるので、通信を止めたまま VBA のイベントループに戻ることに
なり、案A より危ない。案C (長さだけ) は N-5 に届かない。

**論点8 が κ の理由** ★N-1 の一覧は「軽くて常に使える道具」として完成している★。
そこにヘッダのイテレータ (1 要求あたり COM 呼び出し 60 回前後) とボディ読み出しを
既定で足すと、その良さを壊す。詳細が要るのは「この 1 本を再現したい」と分かって
からなので opt-in が素直。

**論点3 が M の理由** 検証のたびに★ログファイルを AI に読ませる運用★。`Cookie` が
平文で載ると、ログを渡した瞬間にセッションが渡る。既定は安全側、外すのは明示的に。

---
### ★N-2 の本丸 ― 見るだけのつもりが通信を壊しうる★

`get_Content` が返す `IStream` は**位置を持つ**。読めば位置が進むので、そのあと
WebView2 が同じストリームから送ると ★空のボディが飛ぶ★。N-1 が「記録だけしてすぐ
返す」で通信物に触れずに済んでいたのに対し、**N-2 は原理的に通信物へ手を入れる**。

対策は `Seek(0, STREAM_SEEK_SET)` で戻すこと (案X)。案Y (`Clone`) は実装しない
ストリームがあり、案Z (`put_Content` で入れ直す) は自前の `IStream` を COM で
実装することになって N-2 が別物の規模になる。

★だがこれは「たぶん大丈夫」で済ませられない★ ―― 自分の側だけを見ても、戻し損ねた
ことは分からない。**送り先が受け取った本文と突き合わせる**しかない (論点9 案D')。

---
### 確定した番号 (すべて SDK ヘッダから)

| 対象 | index |
|---|---|
| `ICoreWebView2WebResourceRequest::get_Content` → `IStream**` | **7** |
| `ICoreWebView2WebResourceRequest::get_Headers` | **9** |
| `ICoreWebView2HttpRequestHeaders::GetIterator` | **8** |
| `...HttpHeadersCollectionIterator::GetCurrentHeader(LPWSTR*, LPWSTR*)` | **3** |
| `...::get_HasCurrentHeader(BOOL*)` / `::MoveNext(BOOL*)` | **4** / **5** |
| `IStream::Read` / `Seek` / `Stat` | **3** / **5** / **12** |

★`IStream` は WebView2.h に無い★ が、**Windows SDK の `objidl.h` から採れた**
(`C:\Program Files (x86)\Windows Kits\10\Include\10.0.26100.0\um\objidl.h`)。
`tools/vtable.py` は環境変数 `WEBVIEW2_H` で読むヘッダを差し替えられるので、
そのまま使える:

```bash
WEBVIEW2_H=".../um/objidl.h" python tools/vtable.py IStream
```

★COM の標準インターフェースでも推測しない★ (設計原則115)。「`Read` は IUnknown の
次だから 3」は合っていたが、合っていたことと確かめたことは別。

---
### 実装 (差し替え 3 本・新規モジュールなし)

| ファイル | 内容 |
|---|---|
| `Wv2Thunks.bas` | `MultiByteToWideChar` の宣言 + `Utf8BytesToString` |
| `Wv2Pane.cls` | 詳細リング + ヘッダ / ボディ読み出し + 伏せ字 (+760 行) |
| `Wv2Tests.bas` | `Test_N2_Detail` / `Test_N2_Help` (+400 行) |

#### 使う側の口

```vba
p.NetDetailOn = True       ' ★詳細は既定 OFF★
Test_N1_Watch              ' 捕捉開始 (N-1 のまま)
' ... 手で操作する ...
Test_N1_Drain              ' まず一覧で当たりを付ける
p.NetDetailAll             ' 詳細を全部出す
p.NetDetail 3              ' 3 番目だけ出す
```

| メンバ | 意味 |
|---|---|
| `NetDetailOn` | ★既定 False★ 詳細を取るか |
| `NetDetailCapacity` | 詳細リングの容量 (既定 50) |
| `NetBodyMaxBytes` | ボディの上限 (既定 65536) |
| `NetRedact` | ★既定 True★ 秘密を伏せるか |
| `AddRedactHeader(name)` / `ClearRedactHeaders()` | 伏せる対象 |
| `NetDetailUriFilter` | 詳細を取る URL の部分一致 |
| `NetDetailCount` / `NetDetailDropped` | 手元の件数 / ★溢れて捨てた件数★ |
| `NetDetailLine(i)` | seq / メソッド / URL / バイト数 / 切った / Content-Type |
| `NetDetailHeaders(i)` / `NetDetailBody(i)` | ヘッダ (伏せ字適用) / ボディ本文 |
| `NetDetail(i)` / `NetDetailAll()` / `NetDetailClear()` | 出す / 全部出す / 捨てる |

#### ★秘密は「出すとき」に伏せる★

リング上には**生のまま**持ち、`NetDetailHeaders` / `NetDetail` が返す/出す瞬間に
伏せる。保存時に伏せてしまうと後から見られなくなるが、この形なら
`NetRedact = False` でその場で解除できる。**平文がディスクへ出るのは
「出す」と書いたときだけ**という性質は保たれる。

既定リストは `cookie` / `set-cookie` / `authorization` / `proxy-authorization` /
`x-api-key` / `x-auth-token` / `x-csrf-token` / `api-key`。

#### UTF-8 のデコードに `MultiByteToWideChar` を使った

★`StrConv(b, vbUnicode)` では駄目★ ―― あれはプロジェクトのコードページ
(日本語環境では CP932) で解釈するので、UTF-8 のバイト列を渡すと化ける。
`ADODB.Stream` は外部 COM なので製品コードでは使えない (検証コードでのみ使用)。
`MultiByteToWideChar` は Win32 API なので、既に使っている `lstrlenW` /
`RtlMoveMemory` と同じ枠に収まる。

---
### 実装前の検算

| 検算 | 結果 |
|---|---|
| `MultiByteToWideChar` を ctypes で実物のまま呼ぶ | **14 件 OK** |
| 伏せ字 / テキスト判定 / ヘッダ取り出しを Python で写す | **24 件 OK** |

★依存する土台そのものを確かめた★ (設計原則103)。`Utf8BytesToString` は
`MultiByteToWideChar` を 2 段 (長さ取得 → 変換) で呼ぶだけなので、ctypes から
同じ順序で呼べば実挙動が分かる。★上限で切ってマルチバイトの途中で切れたとき★ の
振る舞いが確認できたのが収穫:

```
先頭 16 バイト → 'abc=1&x=日本[U+FFFD]'   ← 手前は読める
先頭 17 バイト → 'abc=1&x=日本語'
```

`MB_ERR_INVALID_CHARS` を付けると**変換ごと失敗**して「切れた本文が丸ごと
読めない」になることも確認した。だから `dwFlags = 0` で正解。

★Edge headless の DOM 検証だけは飛んだ★ (Edge 起動中でハンドオフ、仕様事実55)。
確かめたかったのは「fetch に文字列ボディを渡したときの Content-Type」で、
これは fetch 仕様で `text/plain;charset=UTF-8` と決まっており、その値が
テキスト判定されることは Python 側で確認済みだった。実機でも想定どおりだった。

---
### 実機検証 ― ★判定 38 件 OK 38 / FAIL 0 (1 回で通った)★

★本丸★:

```
1  POST  https://httpbingo.org/post  133  False  application/x-www-form-urlencoded
ボディ = [probe=wv2-n1-net&nihongo=%E6%97%A5%E6%9C%AC%E8%AA%9E...]
[OK] ★★送り先にボディが壊れずに届いている★★
[OK]   ★日本語も壊れずに届いている★
```

**`Seek(0)` が効いている。読んでも通信は壊れない。**
`Seek` の `LARGE_INTEGER` 値渡しも `dcf` の `vbLongLong` で通った。

その他:

```
(2) 1  POST  http://127.0.0.1:59999/n2b    36  False  text/plain;charset=UTF-8
    ボディ = [probe=wv2-n2-local&nihongo=日本語]
(3) 1  POST  http://127.0.0.1:59999/n2big 100  True   text/plain;charset=UTF-8
(4) User-Agent: <伏せた 125 文字>
```

★ログから 2 つ発見★

- **フォーム POST の日本語は percent-encoded で載る** (`%E6%97%A5%E6%9C%AC…`)。
  `x-www-form-urlencoded` の仕様どおりだが、★N-5 で PowerShell を吐くときに効く★
- `sec-ch-ua` / `sec-ch-ua-mobile` / `sec-ch-ua-platform` (Client Hints) も見える。
  再現に要るヘッダは揃っている
- 仮想ホストのページから出た要求の `Origin` は `https://appassets.netprobe` になる

---
### 途中で直したもの ― ★自分で見つけた原則との不整合★

詳細リングだけ溢れを黙って上書きしていた。一覧に `NetLogDropped` があるのに
詳細に無いのは不整合なので、`NetDetailDropped` を足し、`NetDetailAll` でも
警告するようにして判定も 1 件足した。**実機に出す前に自分で気づけた分、安い。**

---
### 確立した設計原則 (118 / 119)

- **設計原則118 (N-2) 観測が対象に触れるとき、「壊していないこと」を
  ★別経路で★ 数える。**
  `IStream` を読むと位置が進む = 観測が通信を変えうる。`Seek(0)` で戻したが、
  ★それが効いている証拠は自分の側からは絶対に得られない★。送り先が受け取った
  本文と突き合わせて初めて分かる。設計原則112 (間接的な指標を使わない) の一段上で、
  ★「自分の観測結果そのものが信用できない」場面の作法★。

- **設計原則119 (N-2) 秘密は「保存するとき」ではなく「出すとき」に伏せる。**
  リング上は生のまま持ち、ログや戻り値にする瞬間に伏せる。保存時に伏せると
  後から見られなくなり、解除の口を作れない。この形なら
  ★平文がディスクへ出るのは「出す」と書いたときだけ★ という性質を保ったまま、
  必要なときに `NetRedact = False` で解除できる。

### 確立した仕様事実 (72 〜 75)

- **仕様事実72** ★`get_Content` の `IStream` は読むと位置が進むが、
  `Seek(0, STREAM_SEEK_SET)` で戻せば通信は壊れない★ (実測。送り先にボディが届いた)。

- **仕様事実73** JS の `fetch()` に**文字列**ボディを渡すと Content-Type は
  `text/plain;charset=UTF-8` になる。★CORS の「単純な要求」なのでプリフライトが
  挟まらない★ ので、到達不能な的でも要求そのものは飛ぶ (検証で使える)。

- **仕様事実74** HTML フォームの POST は `application/x-www-form-urlencoded` で、
  ★日本語は percent-encoded で載る★ (`日本語` → `%E6%97%A5%E6%9C%AC%E8%AA%9E`)。

- **仕様事実75** `MultiByteToWideChar(CP_UTF8, 0, ...)` は途中で切れたマルチバイトを
  U+FFFD にして**残りを返す**。★`MB_ERR_INVALID_CHARS` を付けると変換ごと失敗する★
  ので、上限で切る用途では付けてはいけない。

---
### N-2 で使えるようになったもの

★**この 1 本を再現するのに要るものが揃った。**★ 残るはレスポンス側と出口。

### 次にやること (→ N-3 で消化した)

- ~~**N-3** レスポンスの**ステータスとヘッダ**~~ → ★完了 (下記 N-3 段階)★
- 宿題: ★`multipart/form-data` (ファイルアップロード) はバイナリ扱いになる★。
  境界文字列で区切られた中身を読むかは N-5 の必要に応じて
- 宿題: `FETCH`(8) のフィルタは実際には使われていない可能性がある (仕様事実70。N-1 から継続)

---
## N-3 段階 ― レスポンスのステータスとヘッダ (完了)

一覧の 1 行に★結果★が付いた。詳細には★応答ヘッダ★が付いた。

```
#   経過ms  メソッド 種別       状態  URL
1       94  GET      XHR          200 https://httpbingo.org/get?k=n3-off
```

### 論点と決定 (8 件を一括合意)

| # | 論点 | 決定 |
|---|---|---|
| 1 | どのレスポンスを記録するか | ★案A: 一覧に既に居る要求に対応するものだけ★ (+ URL で絞る口) |
| 2 | 要求との突き合わせキー | 案P: URI + メソッドで未対応の要求に付ける |
| 3 | どこに書くか | ★案S: 一覧の行にステータスを足す★ |
| 4 | レスポンスヘッダ | 案U: 詳細エントリに足す (`NetDetailOn` のときだけ) |
| 5 | 溢れた後に来た応答 | 案W: 捨てて★数える★ |
| 6 | リダイレクト | 案Y: 1 要求 1 応答 (埋まっていたら次の未対応へ) |
| 7 | まだ来ていない応答 | ★案α: 空欄にする★ (`0` は本物と見分けが付かない) |
| 8 | 検証 | 両方 (httpbingo + 到達不能ローカルは「来ないこと」の対照) |

---
### ★N-3 の中心問題 ― このイベントにはフィルタが無い★

`ICoreWebView2_2` の vtable に `AddWebResourceResponseReceivedFilter` は
★存在しない★ (61/62 の次は `NavigateWithWebResourceRequest`)。つまり
**画像も CSS もフォントも全部来る**。しかも args が持つのは `Request` と
`Response` だけで、★種別 (`COREWEBVIEW2_WEB_RESOURCE_CONTEXT`) が入っていない★
ので種別で絞ることもできない。

そこで ★一覧に既に居る要求に対応するものだけ記録する (論点1 案A)★。
N-1 の網が「見たいもの」を既に定義しているので、それに乗る。全部欲しくなったら
`NetCaptureStart "ALL"` で一覧側を広げれば応答も自動でついてくる。
★絞り方を二重に持たない★

一方で ★N-3 は通信物に触らない★ (本文 `GetContent` は非同期 = N-4)。
N-2 の「読むと壊す」リスクは無い。

---
### 確定した番号 (すべて SDK ヘッダから)

| 対象 | index |
|---|---|
| `ICoreWebView2_2::add_WebResourceResponseReceived` | **61** (★`m_pView2` で叩く★) |
| 同 `remove_` | **62** |
| `...ResponseReceivedEventArgs::get_Request` / `get_Response` | **3** / **4** |
| `ICoreWebView2WebResourceResponseView::get_Headers` | **3** |
| 同 `get_StatusCode`(int*) / `get_ReasonPhrase` | **4** / **5** |
| 同 `GetContent` (★非同期・完了ハンドラが要る★) | **6** (N-4) |
| `ICoreWebView2HttpResponseHeaders::GetIterator` | ★**7**★ |

IID は `{7de9898a-24f5-40c3-a2de-d4f458e69828}`、`Invoke(sender, args)` の 2 引数。

#### ★踏みかけた罠 ― 要求側と応答側で GetIterator の index が違う★

| インターフェース | GetIterator |
|---|---|
| `ICoreWebView2HttpRequestHeaders` | **8** |
| `ICoreWebView2HttpResponseHeaders` | ★**7**★ |

応答側には `put_` 系が無く `AppendHeader` が先頭に来るので**並びがずれる**。
N-2 のコードを「同じだろう」で流用していたら ★1 つずれて `GetHeaders` を
呼んでいた★ (しかも `GetHeaders` は引数が違うのでクラッシュしうる)。
設計原則115 の生きた実例。

イテレータの型自体はどちらも `ICoreWebView2HttpHeadersCollectionIterator`
(3 = `GetCurrentHeader` / 4 = `get_HasCurrentHeader` / 5 = `MoveNext`) なので、
読み出しは `NetReadHeadersIter(pHeaders, iterIndex)` に切り出して
★index を引数にして呼び分ける★形にした。

---
### 実装 (差し替え 4 本・新規モジュールなし)

| ファイル | 内容 |
|---|---|
| `Wv2Thunks.bas` | `HK_WebResourceResponseReceived = 12` + IID |
| `ComCallbackHandler.cls` | `Case` 追加 → `View2_OnWebResourceResponseReceived` |
| `Wv2Pane.cls` | 一覧に状態欄 + 応答ヘッダ + 突き合わせ |
| `Wv2Tests.bas` | `Test_N3_Status` / `Test_N3_Help` |

#### 追加した Public メンバ

| メンバ | 意味 |
|---|---|
| `NetLogStatus(i)` | その行のステータス (★0 = まだ来ていない★) |
| `NetDetailRespHeaders(i)` | 応答ヘッダ (伏せ字を適用して返す) |
| `NetRespOn` | 応答イベントを拾えているか |
| `NetRespUnmatched` | ★一覧に対応が無かった応答の数★ |

`NetLogLine` は 5 項目 → 7 項目になったが、★ステータスは末尾に足した★ ので
既存の添字 (0～4) は動いていない (N-1 / N-2 のテストは無改修で通る)。
人が読む `NetLogFormat` の方では F12 と同じく種別と URL の間に置いた。

---
### 実装前の検算 ― ★合意した方針の誤りが 1 件見つかった★

突き合わせの中心アルゴリズムを Python で回した (18 件 OK)。

論点2 で「未対応の**最新**の要求に付ける」と提案して承認をもらっていたが、
**回してみたら誤りだった**:

```
同じ URL が 2 本飛んでから応答が返る (Req1 → Req2 → Resp(200) → Resp(404))
    最新から → [(1, 404), (2, 200)]   ← ★入れ替わる★
    最古から → [(1, 200), (2, 404)]   ← 正しい
```

順番に 1 往復ずつの場面では**どちらも同じ結果**なので、
★最古 (FIFO) が常に同等以上★。実装前に最古へ直した。

★合意した内容でも、実装前に回せば直せる★ ―― 実機に出してから気づいたら
「なぜか順番が入れ替わる」という一番読み解きにくい形で出ていた。

ほかに `httpbingo` のエンドポイント (`/status/404` / `/redirect/2` /
`/response-headers` / `/cookies/set`) を実際に叩いて挙動を確かめてから走らせた。

---
### 実機検証 ― ★4 回かかった。だが実装は 1 度も間違っていなかった★

| 回 | 結果 | 原因 |
|---|---|---|
| 1 | 22 件中 FAIL 2 | 応答ヘッダが空 |
| 2 | 同上 (計器を追加) | ★到達点が判明: `[応答] まだ来ていない`★ |
| 3 | 22 件 OK 22 | ★テストの待ち方が誤り★ (実装は無罪) |
| 4 | ★25 件 OK 25★ | 走らなかった判定を埋めた |

#### 1 回目 ― 中心部分は一発で通った

```
1    78  GET  XHR  https://httpbingo.org/redirect/2?k=n3-redir      302
2   609  GET  XHR  https://httpbingo.org/relative-redirect/1        302
3  1156  GET  XHR  https://httpbingo.org/get                        200
[OK] ★どの行にも応答が 1 つずつ付いている★
```

★リダイレクト 3 段が 1 要求 1 応答で綺麗に並んだ★ ―― 突き合わせ (FIFO) が
実地で効いている。200 / 404 / 500 も付き、到達不能な的は空欄のまま。

FAIL は応答ヘッダの 2 件だけだった。

#### 2 回目 ― ★計器を入れて 1 回で切り分けた★

「空だった」だけでは、どこで空になったのかが分からない。ハンドラの中なので
ログは撃てないので、★理由を戻り値の文字列そのものに書き込む★ 計器を入れた
(設計原則111 の形)。あわせてテストで `NetDetail` を呼び、到達点を見えるようにした。

結果は ★`[応答] まだ来ていない`★ ―― `NetAttachRespHeaders` に到達していない。
つまり**ヘッダ読み出しは無罪**。用意した診断文字列は 1 つも出番が無かった。

#### 3 回目 ― ★原因は私のテストの待ち方だった★

タイムスタンプが決め手:

```
23:11:08.694  (4) 開始
23:11:09.035  詳細が取れる     ← 0.34 秒後
23:11:09.037  応答ヘッダ: 空
```

httpbingo への往復は (1) で 0.94 秒、(3) で 6 秒かかっている。0.34 秒では
応答は返っていない。(4) は `N2Wait` (**詳細が現れる**まで) で待っていた。
ところが ★詳細は要求が飛んだ瞬間に作られる★ ので、これは「応答が来た」ことを
何ひとつ意味しない。(1) では正しく `N3Wait` (**ステータスが付く**まで) を
使っていたのに、(4) で取り違えた。

`N3WaitAny` (ステータスを問わず応答が付くまで) を足して直したら、
応答ヘッダは一発で読めた:

```
[応答ヘッダ 10 個]
  access-control-allow-origin: https://appassets.netprobe
  content-type: application/json; charset=utf-8
  x-n3-test: hello          ← ★指定したヘッダが返ってきている★
```

★`GetIterator = 7` は正解で、実装は最初から正しかった。★

#### 4 回目 ― ★OK でも「走らなかった」判定は空振りと同じ★

3 回目は「OK 22 / FAIL 0」だったが、★`Set-Cookie` の判定は 1 つも
走っていなかった★。応答が来ずに `If` ブロックごと飛んでいたのに、
それが数字のどこにも現れなかった。

- 外部の `Set-Cookie` に頼らず、★既に読めている `content-type` を伏せ対象に
  足して、同じ 1 件を伏せる前と後で読み比べる★ 形に変えた
- `Set-Cookie` は上乗せ扱いにして、★走らなかったら警告を出す★

```
[OK] ★応答ヘッダにも伏せ字が効く★
[OK] ★中身 (application/json) が消えている★
[OK] ★リング上は生のまま (NetRedact = False で戻る)★
※ ★Set-Cookie の上乗せ判定は走らなかった (応答が来ない)★
```

3 つ目が効いている ―― ★保存時に伏せていたら `NetRedact = False` でも
戻らない★ ので、これで「出すときに伏せている」(設計原則119) が確定した。

---
### 確立した設計原則 (120 / 121)

- **設計原則120 (N-3) 「用意ができた」を「終わった」の代わりに待たない。
  ★待つ条件は、見たい出来事そのものにする★。**
  詳細エントリは★要求が飛んだ瞬間★に作られ、応答はその後に来る。
  「詳細が現れるまで」待って応答ヘッダを読んでいたので、毎回まだ来ていない
  ものを読んでいた。★近くにある観測しやすい出来事で代用すると、
  失敗が「値が空」という一番読み解きにくい形で出る★。
  設計原則112 (間接的な指標を使わない) の時間版。

- **設計原則121 (N-3) ★走らなかった判定は FAIL より危険★。
  条件で飛ばした判定は「OK 0 / FAIL 0」として数字から消える。
  飛ばしたことを必ずログに出す。**
  N-3 の 3 回目は「OK 22 / FAIL 0」だったが、確かめたかった `Set-Cookie` は
  1 件も走っていなかった。★合格に見えて何も確かめていない★ のが一番たちが悪い。
  設計原則116 (対照を混ぜる) / N-1 の空振り 4 件と同じ根。

### 確立した仕様事実 (76 ～ 78)

- **仕様事実76** ★`ICoreWebView2HttpResponseHeaders::GetIterator` は 7、
  `ICoreWebView2HttpRequestHeaders` は 8★。応答側には `put_` 系が無く
  `AppendHeader` が先頭に来るので並びがずれる。流用すると 1 つずれる。

- **仕様事実77** ★`WebResourceResponseReceived` にはフィルタが無い★
  (`AddWebResourceResponseReceivedFilter` が vtable に存在しない)。
  画像も CSS も全部来る。しかも args に種別が入っていないので種別でも絞れない。

- **仕様事実78** HTTP/2 の応答には reason phrase が存在しないので
  `get_ReasonPhrase` は空文字を返す (ステータスコードだけが来る)。
  `200` の後ろが空なのは異常ではない。

---
### N-3 で使えるようになったもの

★**「いつ・何を・どう叩いて・どうなったか」が一覧の 1 行で完結する。**★
残るはレスポンス本文 (N-4) と出口 (N-5)。

### 次にやること

- **N-4** レスポンス**本文** (`ICoreWebView2WebResourceResponseView::GetContent` = 6)。
  ★これは非同期★ ―― 完了ハンドラ
  (`ICoreWebView2WebResourceResponseViewGetContentCompletedHandler`) が要る。
  N-1～N-3 の「ハンドラの中で同期的に読む」とは作りが変わる。
- 宿題: ★`fetch(..., {redirect:'manual'})` の cross-origin 要求は
  `WebResourceResponseReceived` に乗らないように見える★ (N-3 で 2 回とも
  応答が来なかった)。★観察であって断定ではない★。N-4 以降で必要になったら調べる。
- 宿題: 診断用の計器 (`(診断: ...)` を返す経路) は残してある。害は無いが、
  N-4 以降で邪魔になったら畳む。
